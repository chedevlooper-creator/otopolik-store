---
phase: 08-admin-content-generator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/contentGenerations.ts
  - src/lib/ai/config.ts
  - src/lib/ai/config.test.ts
  - src/lib/ai/content-style-guide.ts
  - src/lib/ai/content-prompt.ts
  - src/lib/ai/content-grounding.ts
  - src/lib/ai/content-grounding.test.ts
autonomous: true
requirements:
  - CNTGEN-01
  - CNTGEN-02
  - CNTGEN-04
  - AIINF-02
must_haves:
  truths:
    - "contentGenerations table stores drafts with pending/ready/approved/rejected/failed statuses and never is a live CMS table (D-02, CNTGEN-02)"
    - "Draft CRUD mutations require adminKey via requireAdminKey and only touch contentGenerations (D-02, D-03)"
    - "Grounding pack is built from products.ts / api.products.* fields plus vehicle-data.ts facts — prices and specs come from structured data, not free invention (D-01, CNTGEN-01)"
    - "CONTENT_STYLE_GUIDE + content system prompt encode premium Turkish Apple/Porsche brand voice as a reusable constant (D-04, CNTGEN-04)"
    - "AiFeature includes content-generator with model/token caps; isAiConfigured remains the graceful gate (D-05, AIINF-02)"
  artifacts:
    - path: convex/schema.ts
      provides: "contentGenerations table + by_status + by_target indexes"
    - path: convex/contentGenerations.ts
      provides: "Admin-key-gated draft list/get/upsert/markStatus — no live CMS writes"
    - path: src/lib/ai/content-style-guide.ts
      provides: "Reusable premium Turkish brand-voice style-guide constant"
    - path: src/lib/ai/content-prompt.ts
      provides: "System prompt composing style guide + per-kind instructions"
    - path: src/lib/ai/content-grounding.ts
      provides: "Structured product/vehicle fact pack builder"
    - path: src/lib/ai/config.ts
      provides: "content-generator AiFeature entry"
  key_links:
    - from: "contentGenerations.* mutations"
      to: "requireAdminKey(adminKey)"
      via: "convex/lib/adminAuth.ts"
    - from: "buildContentGroundingFacts"
      to: "getProductBySlug / vehicle-data helpers"
      via: "src/lib/ai/content-grounding.ts"
    - from: "CONTENT_SYSTEM_PROMPT"
      to: "CONTENT_STYLE_GUIDE"
      via: "src/lib/ai/content-prompt.ts"
---

<objective>
Stand up draft storage, premium Turkish style-guide prompts, and structured product/vehicle grounding — with zero live CMS mutation surface in this plan.

Purpose: Satisfy CNTGEN-01 grounding, CNTGEN-02 draft isolation, CNTGEN-04 brand voice, and AIINF-02 feature gating before any generate/publish UI exists.
Output: `contentGenerations` schema + admin-key draft API, style-guide/prompt constants, grounding builder + vitest, `content-generator` AiFeature.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-admin-content-generator/08-CONTEXT.md
@.planning/phases/05-ai-infrastructure-vehicle-matcher/05-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@convex/schema.ts
@convex/lib/adminAuth.ts
@convex/cms.ts
@convex/files.ts
@convex/products.ts
@src/lib/ai/config.ts
@src/lib/ai/config.test.ts
@src/lib/ai/support-prompt.ts
@src/lib/products.ts
@src/lib/vehicle-data.ts
@src/lib/catalog.ts
@CLAUDE.md
</context>

<decision_map>
CONTEXT Implementation Decisions (D-IDs for coverage; honor exactly):
- D-01: Generate Turkish product descriptions, SEO title/meta, and FAQ copy grounded in structured product + vehicle facts (CNTGEN-01)
- D-02: Draft-then-publish — drafts clearly separated from live CMS; generation path must not mutate live content (CNTGEN-02)
- D-03: Admin-key gating via requireAdminKey on every new Convex mutation; both Next admin-key paths exist for later plans (CNTGEN-03)
- D-04: Written premium Turkish Apple/Porsche style-guide as reusable system-prompt constant (CNTGEN-04)
- D-05: Reuse Phase 5 AI config/kill-switch patterns; generator feature gated by isAiConfigured (AIINF-02)
- D-06: Admin styling remains outside .premium-site (UI in Plan 02)

Planner discretion (CONTEXT Claude's Discretion + orchestrator reuse constraint):
- Persist drafts in Convex `contentGenerations` (research name) with kinds: product_description | product_seo | faq
- Generation transport deferred to Plan 02 as Next.js admin route reusing Phase 5 `getLanguageModel` — research's Convex `"use node"` internalAction targeted overnight batch (CONTEXT deferred); interactive single-item generate must reuse the existing Next AI client without duplicating Anthropic setup into Convex
- This plan creates draft-only Convex APIs; publish-to-live is Plan 02
</decision_map>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: contentGenerations schema + admin-key draft mutations</name>
  <files>convex/schema.ts, convex/contentGenerations.ts</files>
  <behavior>
    - Table contentGenerations fields: kind (product_description | product_seo | faq), targetSlug string, prompt string, draft optional string, status (pending | ready | approved | rejected | failed), failureReason optional, createdAt number, updatedAt number
    - Indexes: by_status on status, by_target on targetSlug (and optionally by_status_updated compound only if needed for list ordering — prefer sort in TS after indexed collect for small admin lists)
    - Public mutations/queries: listRecent, getById, upsertDraft (create/update draft text + status ready/pending/failed), markStatus — every public function takes adminKey: v.string(), calls requireAdminKey, defines args + returns validators
    - Upsert/markStatus/list/get only read/write contentGenerations — handlers must not insert/patch/delete contentSections, faqItems, siteSeo, contentPages, or products
    - listRecent returns newest-first for admin review (createdAt/updatedAt sort in handler after indexed query)
  </behavior>
  <action>
    Per D-02 and D-03: Add `contentGenerations` to `convex/schema.ts` matching research shape (kinds product_description / product_seo / faq; statuses pending/ready/approved/rejected/failed). Indexes by_status and by_target.

    Create `convex/contentGenerations.ts` mirroring `convex/files.ts` admin-key gate pattern:
    - listRecent({ adminKey, limit? }) → array of drafts
    - getById({ adminKey, id }) → draft | null
    - upsertDraft({ adminKey, id?, kind, targetSlug, prompt, draft?, status, failureReason? }) → id
    - markStatus({ adminKey, id, status, failureReason? }) → null

    Do not add a publish mutation in this plan. Do not import or call cms/products write helpers from this module. Await all db ops. No Date.now() inside queries (mutations may stamp updatedAt).

    After schema change, ensure Convex codegen path remains valid (`npx convex codegen` or existing dev watcher) so `_generated` types include the table.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Schema + draft-only admin-key Convex API exist; no live CMS writes in contentGenerations module (CNTGEN-02).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Style guide, content prompt, AiFeature</name>
  <files>src/lib/ai/content-style-guide.ts, src/lib/ai/content-prompt.ts, src/lib/ai/config.ts, src/lib/ai/config.test.ts</files>
  <behavior>
    - CONTENT_STYLE_GUIDE is a non-empty Turkish/English instruction block naming premium register, Turkish-only output, and banned salesy/spammy phrasings appropriate to Apple/Porsche luxury tone
    - CONTENT_SYSTEM_PROMPT (or buildContentSystemPrompt(kind)) includes the style guide and per-kind rules for product_description, product_seo, faq; instructs model to use only provided grounding facts for specs/prices
    - AiFeature union includes content-generator; AI_MODEL_IDS uses a quality-focused Claude id (prefer research pin claude-opus-4-8 or documented Sonnet if opus unavailable in project — document choice in SUMMARY); getAiMaxTokens returns a generous cap suitable for long Turkish copy (e.g. 2048–4096)
    - Existing config tests still pass; new assertions cover content-generator model/token lookups
  </behavior>
  <action>
    Per D-04 and D-05: Add `content-style-guide.ts` exporting `CONTENT_STYLE_GUIDE` as the single reusable brand-voice constant. Add `content-prompt.ts` that composes that constant into the content-generator system prompt and kind-specific output contracts (description prose; SEO title+meta JSON; FAQ question+answer JSON). Explicitly forbid inventing prices/specs not present in the grounding block.

    Extend `AiFeature` / `AI_MODEL_IDS` / `AI_MAX_TOKENS` in `config.ts` with `content-generator`. Keep `isAiConfigured` / kill-switch behavior unchanged for AIINF-02. Update `config.test.ts` accordingly.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/config.test.ts</automated>
  </verify>
  <done>Style guide + prompt constants and content-generator feature config land (CNTGEN-04, AIINF-02).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Structured product/vehicle grounding builder</name>
  <files>src/lib/ai/content-grounding.ts, src/lib/ai/content-grounding.test.ts</files>
  <behavior>
    - buildContentGroundingFacts({ kind, targetSlug }) returns a structured object/string pack including product name, brand, model, category, price, features, setContents, optionalExtras, compatibility when a product slug resolves via getProductBySlug (and/or catalog Convex-first helper if already used elsewhere — prefer existing catalog/products read patterns)
    - When kind needs vehicle context and product brand/model map into vehicle-data, include getVehiclePrice / model metadata from vehicle-data.ts; never fabricate a price not returned by those helpers
    - Unknown slug yields a clear empty/error-shaped result the caller can refuse — does not invent a product
    - Module does not import Convex mutation helpers or write APIs for cms/products live tables
  </behavior>
  <action>
    Per D-01: Implement `buildContentGroundingFacts` that reads structured facts from `src/lib/products.ts` (`getProductBySlug` / seed catalog) and `src/lib/vehicle-data.ts` (`getVehiclePrice`, model lookups). If `src/lib/catalog.ts` already exposes Convex-first product reads suitable for server use, prefer that with static fallback (project pattern) — verify exports before inventing names.

    Serialize a compact fact block suitable for prompt injection (JSON or labeled Turkish field list). Unit-test: known slug includes real price/features from catalog; unknown slug does not invent specs; vehicle price equals getVehiclePrice for a known brand/model pair.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/content-grounding.test.ts</automated>
  </verify>
  <done>Grounding builder proven with vitest; facts sourced from product/vehicle code (CNTGEN-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → Convex draft API | adminKey must be verified server-side; unauthenticated callers must fail |
| Grounding builder → LLM prompt (later) | Only structured catalog facts cross into generation context |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | contentGenerations mutations | high | mitigate | requireAdminKey on every public function; args validators |
| T-08-02 | Tampering | Draft upsert | medium | mitigate | Draft module cannot patch live CMS/product tables; publish isolated to Plan 02 |
| T-08-03 | Information Disclosure | Grounding pack | low | accept | Pack contains already-public storefront product facts |
| T-08-SC | Tampering | npm installs | high | mitigate | No new packages in this plan — reuse existing stack |
</threat_model>

<verification>
- `npm run typecheck` passes
- `npx vitest run src/lib/ai/config.test.ts src/lib/ai/content-grounding.test.ts` passes
- Grep `convex/contentGenerations.ts` for live-table write identifiers should find none (contentSections/faqItems/siteSeo/products db writes)
</verification>

<success_criteria>
Draft storage + style guide + grounding exist; generation and publish UI still absent; CNTGEN-01/02/04 foundations ready for Plan 02.
</success_criteria>

<output>
Create `.planning/phases/08-admin-content-generator/08-01-SUMMARY.md` when done
</output>
