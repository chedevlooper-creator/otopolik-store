---
phase: 06-streaming-chat-configurator-assistant
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - package.json
  - package-lock.json
  - src/lib/mat-colors.ts
  - src/components/configurator/MatConfigurator.tsx
  - src/components/configurator/ConfiguratorAssistantProvider.tsx
  - src/components/configurator/ConfiguratorChat.tsx
  - src/app/olusturucu/page.tsx
autonomous: true
requirements:
  - CFGAI-01
  - CFGAI-02
  - CFGAI-03
  - CFGAI-04
  - CFGAI-05
must_haves:
  truths:
    - "User can complete vehicle → colors → extras → price → WhatsApp handoff in Turkish streaming chat on /olusturucu"
    - "Tool calls visibly update the same MatConfigurator stepper state the user would edit manually (CFGAI-02)"
    - "add_to_cart path calls useCart().addItem with price === calculateMatPrice for the active extras (CFGAI-03)"
    - "Chat UI uses surface-glass/mac-glass, Racing Red, Turkish copy, mobile-first, respects prefers-reduced-motion (CFGAI-04)"
    - "Header/label shows AI Asistan and never claims to be a human representative (CFGAI-05)"
    - "When isAiConfigured() is false, chat entry is hidden; manual stepper still works (D-07)"
  artifacts:
    - path: src/components/configurator/ConfiguratorChat.tsx
      provides: "Premium streaming chat panel with error/reconnect UX"
    - path: src/components/configurator/ConfiguratorAssistantProvider.tsx
      provides: "Shared configurator state bridge for AI tools + MatConfigurator"
    - path: src/components/configurator/MatConfigurator.tsx
      provides: "Stepper driven by shared state (manual + AI)"
  key_links:
    - from: "ConfiguratorChat onToolCall"
      to: "ConfiguratorAssistantProvider setters + useCart().addItem"
      via: "client tool handlers"
    - from: "prepare_whatsapp_handoff"
      to: "buildWhatsAppLink(siteConfig.whatsappNumber, message)"
      via: "src/lib/whatsapp.ts (user-gesture open)"
    - from: "/olusturucu aiEnabled"
      to: "ConfiguratorChat mount gate"
      via: "isAiConfigured() server prop"
---

<objective>
Wire a premium Turkish streaming chat panel into `/olusturucu` that drives the REAL MatConfigurator via Plan 01 tools, adds a correctly priced line to the real cart, and hands off to WhatsApp — with mobile stream error/retry UX.

Purpose: Deliver CFGAI-01…05 end-to-end customer experience on top of the Plan 01 route.
Output: Shared configurator state provider, ConfiguratorChat UI, MatConfigurator refactor to consume shared state, package `@ai-sdk/react` for useChat.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-streaming-chat-configurator-assistant/06-CONTEXT.md
@.planning/phases/06-streaming-chat-configurator-assistant/06-01-PLAN.md
@.planning/research/PITFALLS.md
@src/components/configurator/MatConfigurator.tsx
@src/components/configurator/VehicleMatchInput.tsx
@src/context/cart-context.tsx
@src/lib/types.ts
@src/lib/mat-pricing.ts
@src/lib/whatsapp.ts
@src/lib/site-config.ts
@src/app/olusturucu/page.tsx
@src/app/globals.css
@CLAUDE.md
</context>

<decision_map>
- D-02: Drive REAL MatConfigurator stepper — shared state, not a parallel checkout UI
- D-03: Cart price === calculateMatPrice output via same addItem shape as handleAddToCart today
- D-05: OLED-black/glass premium chat (surface-glass / mac-glass, Racing Red, Syne/Instrument Sans)
- D-06: Labeled AI Asistan; never human impersonation
- D-07: Hide chat when AI unconfigured
- D-08: WhatsApp via buildWhatsAppLink; draft-then-user-sends; open on user gesture (popup-blocker safe)
Discretion locked: Embed chat on `/olusturucu` (panel beside/above configurator), not a site-wide SiteChrome floater (Phase 7 can reuse patterns). Streaming drop → Turkish error + Yeniden dene, clear truncated bubble.
</decision_map>

<tasks>

<task type="auto">
  <name>Task 1: Shared configurator state bridge for AI + manual stepper</name>
  <files>src/lib/mat-colors.ts, src/components/configurator/ConfiguratorAssistantProvider.tsx, src/components/configurator/MatConfigurator.tsx</files>
  <action>
    Per D-02/D-03: Extract FLOOR_COLORS / EDGE_COLORS (and ColorSwatch-compatible shapes) into `src/lib/mat-colors.ts` if Plan 01 did not already — MatConfigurator imports from there.

    Create `ConfiguratorAssistantProvider` (`"use client"`) that owns the stepper state currently local to MatConfigurator: vehicle details, floor, edge, heelPad, trunkMat, floorTouched, edgeTouched, plus helpers:
    - applyVehicle / applyFloor / applyEdge / applyExtras (used by chat tool handlers)
    - buildCartItem(): returns CartItem identical in shape to today's handleAddToCart (slug, name, image from preview lookup, price: calculateMatPrice({ heelPad, trunkMat }), color summary, configuration object)
    - currentStep derivation must match existing MatConfigurator logic so the progress UI advances when AI fills steps

    Refactor MatConfigurator to consume this context for state/setters while preserving all existing manual UX (VehicleSelector, VehicleMatchInput when aiEnabled, ColorPicker, extras, add-to-cart buttons). Manual edits and AI tools MUST mutate the same state object so CFGAI-02 is observable (stepper highlights advance when chat sets colors/vehicle).

    Do not invent a second price path — always calculateMatPrice. Do not call Convex orders from this provider.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>MatConfigurator and AI tools share one stepper state; buildCartItem price equals calculateMatPrice (CFGAI-02/03).</done>
</task>

<task type="auto">
  <name>Task 2: Premium ConfiguratorChat with streaming + reconnect UX</name>
  <files>package.json, package-lock.json, src/components/configurator/ConfiguratorChat.tsx</files>
  <action>
    Per D-05/D-06 and research Pitfall 5: Install `@ai-sdk/react` at a version paired with existing `ai@^7` (verify npm peer range; official Vercel package). Use `useChat` against `/api/ai/chat` (DefaultChatTransport or equivalent AI SDK v7 API).

    Build `ConfiguratorChat.tsx` (`"use client"`):
    - Visible title/badge: "AI Asistan" (CFGAI-05). Supporting copy in Turkish clarifying it is yapay zekâ, not a human satış temsilcisi. Never use human-agent phrasing.
    - Layout: mobile-first panel using `surface-glass` and/or `mac-glass`, Racing Red accents (`btn-red-rich` / brand-red), OLED black surfaces — must feel native inside `.premium-site`, not a white third-party widget (CFGAI-04).
    - Stream assistant tokens into bubbles; respect `prefers-reduced-motion` (no noisy loop animations; disable sheen/motion when reduced).
    - Handle client tools from Plan 01 via onToolCall / addToolOutput: set_vehicle, set_floor_color, set_edge_color, set_extras → provider setters; add_to_cart → useCart().addItem(buildCartItem()) only when vehicle complete; prepare_whatsapp_handoff → store draft, show CTA that opens `buildWhatsAppLink(siteConfig.whatsappNumber, draft)` synchronously on click/tap (D-08; do not window.open from async tool completion alone).
    - Streaming error/reconnect UX (research-flagged): on stream failure/abort/network drop, replace truncated assistant bubble with a clear Turkish error state and a "Yeniden dene" action that resends or continues safely; support AbortController cancel; show subtle typing/streaming indicator only while active.
    - Rate-limit (429) and ai_unavailable (503) map to Turkish messages consistent with VehicleMatchInput tone.

    Keep the component scoped to configurator — no SiteChrome global float in this phase.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Streaming chat UI mounts with AI Asistan labeling, premium styling, tool→state wiring, cart + WhatsApp handoff, and stream retry UX (CFGAI-01/03/04/05, D-05/D-06/D-08).</done>
</task>

<task type="auto">
  <name>Task 3: Mount chat on /olusturucu behind AI gate</name>
  <files>src/app/olusturucu/page.tsx, src/components/configurator/MatConfigurator.tsx, src/components/configurator/ConfiguratorAssistantProvider.tsx</files>
  <action>
    Per D-07: Wrap configurator page content with ConfiguratorAssistantProvider. Render ConfiguratorChat only when `aiEnabled` / `isAiConfigured()` is true (same server gate already passed into MatConfigurator). When false, chat is absent and manual stepper unchanged.

    Compose layout so chat and stepper are both visible on desktop (e.g. stacked on mobile, side-by-side or chat-above on larger screens) without breaking MatConfigurator gallery/preview. Preserve existing searchParams prefill props.

    Ensure VehicleMatchInput and AI chat both update the shared vehicle state when present.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>/olusturucu shows gated AI chat driving the real stepper; unconfigured builds hide chat only (CFGAI-01/02, D-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chat tool intents → React state | Client must validate palette names / vehicle completeness before cart add |
| add_to_cart tool → useCart | Must not accept model-supplied price numbers — recompute via calculateMatPrice |
| WhatsApp CTA → wa.me | User gesture required; AI drafts text only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Tampering | add_to_cart handler | high | mitigate | Ignore any model-provided price field; always buildCartItem() + calculateMatPrice |
| T-06-07 | Spoofing | ConfiguratorChat chrome | medium | mitigate | Persistent AI Asistan label + disclosure copy (CFGAI-05) |
| T-06-08 | Denial of service | useChat retries | medium | mitigate | Bound auto-retries; explicit user Yeniden dene; respect 429 Retry-After |
| T-06-09 | Information disclosure | rendered markdown | low | mitigate | Prefer plain text / safe rendering; no raw HTML from model |
| T-06-SC | Tampering | @ai-sdk/react install | low | accept | Official Vercel scope; confirm package page at install |
</threat_model>

<verification>
- `npm run typecheck` passes
- Focused eslint on new configurator chat files
- Manual smoke (Plan 03 checkpoint): stream, stepper advance, cart price, WhatsApp CTA, AI-off fallback
</verification>

<success_criteria>
- Full Turkish chat flow drives MatConfigurator and cart with correct price
- UI matches premium system and discloses AI Asistan
- Stream errors offer retry without stuck truncated bubbles
</success_criteria>

<output>
Create `.planning/phases/06-streaming-chat-configurator-assistant/06-02-SUMMARY.md` when done
</output>
