---
phase: 07-support-order-helper-grounding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/cms.ts
  - src/lib/ai/config.ts
  - src/lib/ai/config.test.ts
  - src/lib/ai/support-grounding.ts
  - src/lib/ai/support-grounding.test.ts
  - src/lib/ai/support-tools.ts
  - src/lib/ai/support-tools.test.ts
  - src/lib/ai/support-prompt.ts
  - src/app/api/ai/support/route.ts
autonomous: true
requirements:
  - SUPAI-01
  - SUPAI-02
  - SUPAI-03
  - SUPAI-05
must_haves:
  truths:
    - "getFaqs and getContentPage read Convex when configured and fall back to cms-defaults when not (SUPAI-01 live path)"
    - "Support grounding tool re-fetches CMS + site-settings on every execute — never embeds shipping/price facts in the system prompt (D-01, SUPAI-01)"
    - "Support system prompt forces scoped Turkish refusal for off-topic questions (D-02, SUPAI-02)"
    - "POST /api/ai/support streams via streamText with Phase 5/6 kill switch, rate limit, and graceful 503 (D-06, AIINF-02/04)"
    - "Support tool module has zero adminKey / requireAdminKey / Convex mutation imports (AIINF-05)"
    - "No Convex chatSessions/chatMessages tables or transcript writes in this plan (D-05, SUPAI-05)"
  artifacts:
    - path: src/lib/ai/support-grounding.ts
      provides: "Answer-time CMS + site-settings fact pack with interpolateCmsText"
    - path: src/lib/ai/support-tools.ts
      provides: "get_support_grounding, get_mat_price, prepare_whatsapp_handoff, draft_order_summary tools"
    - path: src/lib/ai/support-prompt.ts
      provides: "Turkish support AI Asistan prompt: grounded answers, scoped refusal, user-sends WhatsApp"
    - path: src/app/api/ai/support/route.ts
      provides: "Streaming support chat route (SSE UI message stream)"
    - path: src/lib/cms.ts
      provides: "Convex-first getFaqs + getContentPage with static fallback"
  key_links:
    - from: "get_support_grounding execute"
      to: "getFaqs + getContentPage(kargo) + getStoreSettings + interpolateCmsText"
      via: "src/lib/ai/support-grounding.ts"
    - from: "getLanguageModel('support-chat')"
      to: "getAnthropicProvider() lazy null"
      via: "src/lib/ai/anthropic-client.ts"
    - from: "streamText tools"
      to: "supportTools (no configurator stepper tools)"
      via: "src/app/api/ai/support/route.ts"
---

<objective>
Stand up live CMS/site-settings grounding, the Turkish support system prompt with scoped refusal, and streaming `POST /api/ai/support` — reusing Phase 5/6 AI client, rate limit, kill switch, and `calculateMatPrice` wrappers. Wire `cms.ts` reads to Convex so admin-edited FAQ/shipping copy is actually live.

Purpose: Satisfy SUPAI-01 (live grounding), SUPAI-02 (refusal in prompt), SUPAI-03 (WhatsApp draft tool surface), SUPAI-05 (stateless — no transcript persistence) before any support UI exists.
Output: Convex-first CMS reads, `support-grounding` + `support-tools` + `support-prompt`, `support-chat` AiFeature, streaming support route, vitest coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-support-order-helper-grounding/07-CONTEXT.md
@.planning/phases/06-streaming-chat-configurator-assistant/06-SUMMARY.md
@.planning/phases/05-ai-infrastructure-vehicle-matcher/05-SUMMARY.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@src/lib/cms.ts
@src/lib/cms-defaults.ts
@src/lib/site-settings.ts
@src/lib/whatsapp.ts
@src/lib/mat-pricing.ts
@src/lib/ai/config.ts
@src/lib/ai/anthropic-client.ts
@src/lib/ai/customer-tools.ts
@src/lib/ai/rate-limit.ts
@src/lib/ai/configurator-tools.ts
@src/lib/ai/configurator-prompt.ts
@src/app/api/ai/chat/route.ts
@convex/cms.ts
@CLAUDE.md
</context>

<decision_map>
CONTEXT Implementation Decisions (D-IDs for coverage; honor exactly):
- D-01: Live grounding — re-fetch cms.ts + site-settings.ts at answer time; never hardcode shipping/price facts into the system prompt (SUPAI-01)
- D-02: Scoped refusal — off-topic / open-domain → short Turkish redirect to mats/orders (SUPAI-02)
- D-03: Uncertain → pre-filled wa.me via whatsapp.ts builder (tool prepares draft; UI opens in Plan 02) (SUPAI-03)
- D-04: Order draft for user review/send — AI never places order or sends message (SUPAI-04; tool contract here, UI gesture in Plan 02)
- D-05: KVKK minimization — no server transcript tables this phase; bounded in-memory/client history only (SUPAI-05)
- D-06: Reuse Phase 5/6 client, rate limit, kill switch, graceful 503, price via getCustomerMatPrice → calculateMatPrice, admin isolation (AIINF-02/03/05)
- D-07: AI Asistan disclosure in prompt (UI chrome in Plan 02)

Planner discretion (CONTEXT Claude's Discretion):
- Separate route `POST /api/ai/support` (not a mode flag on `/api/ai/chat`) — keeps configurator tools out of support and vice versa
- Single server tool `get_support_grounding` that packs FAQs + kargo page sections + store shipping settings (re-fetched every execute), plus reused `get_mat_price` and WhatsApp draft tools — not multiple overlapping fact tools
- Do NOT create Convex chatSessions/chatMessages (deferred in CONTEXT)
</decision_map>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire live CMS reads + answer-time grounding pack</name>
  <files>src/lib/cms.ts, src/lib/ai/support-grounding.ts, src/lib/ai/support-grounding.test.ts</files>
  <behavior>
    - When Convex is configured, getFaqs returns published items from api.cms.listFaqs mapped to FaqItem; on unset Convex / query error returns DEFAULT_FAQS with source fallback
    - When Convex is configured, getContentPage(slug) uses api.cms.getPageBySlug + api.cms.listSectionsByPage; on failure returns getDefaultPage/getDefaultSections with source fallback
    - buildSupportGroundingFacts (or equivalent) awaits getFaqs + getContentPage("kargo") + getStoreSettings and applies interpolateCmsText with freeShippingThreshold / shippingFee / estimatedDispatch tokens — result includes those numeric/string facts from the live settings object
    - Calling buildSupportGroundingFacts twice with different mocked getStoreSettings values yields different freeShippingThreshold in the pack (freshness — never caches settings in module scope)
    - support-grounding.ts does not import adminKey, requireAdminKey, or Convex mutation helpers
  </behavior>
  <action>
    Per D-01: Today `getFaqs` / `getContentPage` always return static fallback even though Convex already exposes `listFaqs`, `getPageBySlug`, and `listSectionsByPage`. Wire both getters to the existing Convex-first + graceful-fallback pattern used by `getSiteSettings` in `src/lib/site-settings.ts` (getConvexClient / isConvexConfigured try/catch → defaults). Map Convex FAQ docs to FaqItem (`id` from `_id`, drop `_creationTime`/`updatedAt` from the public shape). Preserve existing save* admin mutation paths unchanged.

    Create `src/lib/ai/support-grounding.ts` (server-only) exporting a pure async builder used by tools:
    - Fetch FAQs via getFaqs
    - Fetch shipping copy via getContentPage("kargo") sections
    - Fetch live settings via getStoreSettings (phoneDisplay, whatsappNumber, freeShippingThreshold, shippingFee, estimatedDispatch, businessHours — not mat prices as authoritative mat totals; mat prices still go through getCustomerMatPrice)
    - Run interpolateCmsText on FAQ answers and section bodies with the live token map
    - Return a structured, JSON-serializable fact pack the model can cite (faqs[], shippingSections[], settings subset). Do not put this pack into the system prompt string at module load — only tool execute output.

    Write `support-grounding.test.ts` that mocks/stubs the three fetchers (or injects dependency seams) to prove: (1) interpolated threshold appears in output, (2) changing threshold between calls changes output, (3) no module-level settings cache. Prefer injectable deps over brittle network mocks if cleaner.
  </action>
  <verify>
    <automated>npm test -- src/lib/ai/support-grounding.test.ts</automated>
  </verify>
  <done>CMS reads are Convex-first with fallback; grounding pack re-fetches and interpolates live settings each call (SUPAI-01, D-01).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Support tools, prompt, AiFeature, streaming /api/ai/support</name>
  <files>src/lib/ai/config.ts, src/lib/ai/config.test.ts, src/lib/ai/support-tools.ts, src/lib/ai/support-tools.test.ts, src/lib/ai/support-prompt.ts, src/app/api/ai/support/route.ts</files>
  <behavior>
    - AiFeature includes support-chat with pinned Sonnet model id (same family as configurator-chat) and max_tokens ≥ 1024
    - get_support_grounding execute calls buildSupportGroundingFacts every time (no stale closure over settings)
    - get_mat_price returns getCustomerMatPrice → equals calculateMatPrice for same extras
    - prepare_whatsapp_handoff / draft_order_summary schemas accept message or structured order fields but do not send WhatsApp, write orders, or accept a model-supplied price override — any stated price in draft_order_summary must be recomputed via getCustomerMatPrice
    - SUPPORT_SYSTEM_PROMPT contains AI Asistan disclosure, forbids inventing kargo/fiyat numbers, requires get_support_grounding before shipping/sizing/care claims, and instructs scoped Turkish redirect for off-topic questions
    - support-tools.ts and support/route.ts fail isolation scans if they import admin auth/mutation surfaces
    - Unset key or AI_FEATURES_ENABLED=false path yields 503 ai_unavailable; rate limit returns 429 (mirror chat route)
  </behavior>
  <action>
    Per D-01/D-02/D-03/D-04/D-05/D-06/D-07: Extend `AiFeature` in `src/lib/ai/config.ts` with `"support-chat"`. Pin the same conversational Sonnet snapshot string already used for `configurator-chat` (verify against existing `AI_MODEL_IDS`). Set chat-appropriate max tokens (≥1024). Update `config.test.ts`.

    Create `src/lib/ai/support-tools.ts` with Vercel AI SDK `tool` + zod:
    - `get_support_grounding` → execute buildSupportGroundingFacts (server)
    - `get_mat_price` → getCustomerMatPrice (reuse customer-tools; AIINF-03)
    - `draft_order_summary` → builds Turkish WhatsApp draft text from vehicle/colors/extras/notes; recomputes price via getCustomerMatPrice; returns { draft, price, currency } — never places an order
    - `prepare_whatsapp_handoff` → schema-only or returns { prepared: true, message } for client CTA (same user-sends contract as configurator); no server-side wa.me fetch

    Do not include configurator stepper tools (set_vehicle, add_to_cart, etc.) on this route.

    Create `src/lib/ai/support-prompt.ts` exporting `SUPPORT_SYSTEM_PROMPT` in Turkish that: (1) identifies as AI Asistan not human (D-07), (2) requires get_support_grounding before answering shipping/sizing/care (D-01), (3) forbids inventing fiyat/kargo/indirim numbers — use tools (D-01/AIINF-03), (4) off-topic → short redirect to paspas seçimi / sipariş / WhatsApp (D-02), (5) uncertain → prepare_whatsapp_handoff (D-03), (6) order summary via draft_order_summary; user sends (D-04), (7) no claim of storing chat history / no training language (D-05).

    Create `src/app/api/ai/support/route.ts` mirroring `src/app/api/ai/chat/route.ts`: runtime nodejs, isAiFeaturesEnabled/isAiConfigured/getLanguageModel("support-chat") → 503, validateUIMessages + MAX_MESSAGES/MAX_USER_MESSAGE_CHARS bounds, checkAiRateLimit + 429, streamText with SUPPORT_SYSTEM_PROMPT + supportTools + getAiMaxTokens("support-chat") + stepCountIs, toUIMessageStreamResponse. No ANTHROPIC_API_KEY in responses. No Convex transcript writes (D-05). Do not import admin auth.

    Write `support-tools.test.ts` covering price-equality, grounding execute freshness hook (spy that builder is awaited), draft_order_summary price recompute, and admin-isolation source scan (mirror configurator-tools.test.ts / customer-tools.test.ts).
  </action>
  <verify>
    <automated>npm test -- src/lib/ai/config.test.ts src/lib/ai/support-tools.test.ts src/lib/ai/support-grounding.test.ts</automated>
  </verify>
  <done>support-chat feature + tools + prompt + streaming route exist with Phase 5/6 guardrails and live grounding (SUPAI-01/02/03/05, D-01…D-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/ai/support | Untrusted Turkish free-text + client message history |
| Support tools → CMS/settings | Read-only storefront facts; must not reach admin mutations |
| Model text → customer | Shipping/price claims must come from tool outputs, not free generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | support-tools / stale prompt facts | high | mitigate | Answer-time get_support_grounding + no shipping numbers in SUPPORT_SYSTEM_PROMPT; eval in Plan 03 |
| T-07-02 | Elevation of privilege | support-tools → admin | high | mitigate | Isolation source scan; no adminKey/Convex mutation imports |
| T-07-03 | Information disclosure | support route | medium | mitigate | Server-only key; isAiConfigured gate; no key in JSON/SSE |
| T-07-04 | Denial of service | support route | high | mitigate | Reuse checkAiRateLimit + max tokens + message size caps |
| T-07-05 | Spoofing | support prompt | medium | mitigate | Prompt forces AI Asistan identity; Plan 02 UI label |
| T-07-06 | Information disclosure | transcript retention | medium | mitigate | No Convex transcript tables; bounded client history only (D-05) |
| T-07-SC | Tampering | npm installs | low | accept | No new packages in this plan — reuse existing ai / @ai-sdk stack |
</threat_model>

<verification>
- `npm test -- src/lib/ai/support-grounding.test.ts src/lib/ai/support-tools.test.ts src/lib/ai/config.test.ts` passes
- `npm run typecheck` passes for new support AI files
- Focused lint on touched `src/lib/ai/**` and `src/app/api/ai/support/**` (ignore pre-existing app lint debt)
</verification>

<success_criteria>
- CMS FAQ/page reads are Convex-first with static fallback
- Support grounding re-fetches live settings/FAQs per tool call
- Streaming `/api/ai/support` reuses Phase 5/6 guardrails with scoped-refusal prompt
- No admin mutation surface or transcript persistence from support tools/route
</success_criteria>

<output>
Create `.planning/phases/07-support-order-helper-grounding/07-01-SUMMARY.md` when done
</output>
