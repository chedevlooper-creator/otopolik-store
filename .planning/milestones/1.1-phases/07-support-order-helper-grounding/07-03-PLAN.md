---
phase: 07-support-order-helper-grounding
plan: 03
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - src/lib/ai/evals/support-chat-golden.ts
  - src/lib/ai/evals/support-chat-eval.test.ts
  - package.json
  - src/components/support/SupportChat.tsx
autonomous: false
requirements:
  - SUPAI-01
  - SUPAI-02
  - SUPAI-03
  - SUPAI-04
  - SUPAI-05
must_haves:
  truths:
    - "Golden eval proves grounding pack tracks a changed freeShippingThreshold / shippingFee (SUPAI-01 freshness)"
    - "Eval asserts SUPPORT_SYSTEM_PROMPT encodes scoped Turkish refusal + AI Asistan disclosure (SUPAI-02)"
    - "Eval asserts support tools/route/UI cannot auto-send WhatsApp or write orders — user-sends-only (SUPAI-03/04)"
    - "Eval asserts no Convex transcript persistence symbols in support route/tools/UI (SUPAI-05)"
    - "npm script ai:eval:support-chat gates regressions without ANTHROPIC_API_KEY"
    - "Human confirms /destek grounding, refusal, WhatsApp user-send, AI-off fallback, premium UI"
  artifacts:
    - path: src/lib/ai/evals/support-chat-golden.ts
      provides: "Golden cases for grounding freshness, refusal, user-sends"
    - path: src/lib/ai/evals/support-chat-eval.test.ts
      provides: "Vitest runner for support chat evals"
  key_links:
    - from: "buildSupportGroundingFacts"
      to: "live settings threshold in pack"
      via: "freshness assertions"
    - from: "package.json ai:eval:support-chat"
      to: "support-chat-eval.test.ts"
      via: "vitest run"
---

<objective>
Lock SUPAI-01…05 with deterministic golden evals (grounding freshness, scoped refusal, user-sends-not-AI, KVKK minimization source scans), polish any /destek UX gaps, and human-verify the Phase 7 success criteria.

Purpose: Prevent stale shipping answers, open-domain drift, and AI-auto-send regressions before Phase 8; mirror Phase 5/6 eval gate pattern.
Output: support-chat golden eval + npm script, optional UX polish, blocking human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-support-order-helper-grounding/07-CONTEXT.md
@.planning/phases/06-streaming-chat-configurator-assistant/06-03-PLAN.md
@src/lib/ai/evals/configurator-chat-golden.ts
@src/lib/ai/evals/configurator-chat-eval.test.ts
@src/lib/ai/support-grounding.ts
@src/lib/ai/support-tools.ts
@src/lib/ai/support-prompt.ts
@src/app/api/ai/support/route.ts
@src/components/support/SupportChat.tsx
@src/app/destek/page.tsx
@package.json
</context>

<decision_map>
- D-01: Grounding-freshness eval mandatory (changed setting must appear in fact pack)
- D-02: Refusal constraints reflected in prompt golden expectations
- D-03/D-04: User-sends-not-AI source + contract scans
- D-05: No transcript table / mutation symbols in support surface sources
Discretion: Keep evals deterministic without live Anthropic calls (mirror configurator-chat eval). Live streaming quality remains human-verified.
</decision_map>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Support chat golden eval + npm script</name>
  <files>src/lib/ai/evals/support-chat-golden.ts, src/lib/ai/evals/support-chat-eval.test.ts, package.json</files>
  <behavior>
    - Grounding freshness: two sequential buildSupportGroundingFacts calls with different injected freeShippingThreshold (and/or shippingFee) produce packs containing the respective values — proves answer-time re-fetch, not module-cached prompt bake
    - getCustomerMatPrice / draft_order_summary price path equals calculateMatPrice for golden extras combos (AIINF-03 carryover)
    - SUPPORT_SYSTEM_PROMPT contains AI Asistan, forbids inventing kargo/fiyat, requires grounding tool before policy answers, and includes off-topic / kapsam dışı redirect instructions (SUPAI-02)
    - Isolation: support-tools.ts, support/route.ts, SupportChat.tsx contain no requireAdminKey / adminKey mutation imports and no orders table writes
    - User-sends-not-AI: support sources do not call window.open for WhatsApp from tool handlers alone; draft tools do not invoke fetch against wa.me; SupportChat exposes an href CTA pattern using buildWhatsAppLink
    - KVKK: support route/tools/UI sources do not reference chatSessions, chatMessages, or transcript persistence APIs
    - Suite passes with ANTHROPIC_API_KEY unset
  </behavior>
  <action>
    Per D-01…D-05: Mirror Phase 6 eval layout. Add `support-chat-golden.ts` with cases covering: (1) ≥2 shipping-threshold freshness fixtures, (2) ≥4 mat price/extras combos for draft integrity, (3) documented off-topic example prompts expected to be refused by policy (prompt-level expectations — no live LLM required), (4) user-sends contract notes.

    Add `support-chat-eval.test.ts` that:
    - Asserts grounding freshness via injectable builder deps or exported test seam from Plan 01
    - Asserts price equality for draft/mat helpers
    - Scans SUPPORT_SYSTEM_PROMPT for disclosure + refusal + no-invention rules
    - Source-scans support-tools, support route, SupportChat for admin isolation, absence of transcript persistence identifiers, and user-send WhatsApp pattern

    Add package.json script `ai:eval:support-chat` running vitest on the new eval file (alongside existing ai:eval:* scripts).

    Do not add a training pipeline or Convex transcript schema.
  </action>
  <verify>
    <automated>npm run ai:eval:support-chat</automated>
  </verify>
  <done>Golden eval passes without API key; freshness, refusal, user-sends, and KVKK scans green (SUPAI-01…05).</done>
</task>

<task type="auto">
  <name>Task 2: /destek polish from eval/smoke gaps</name>
  <files>src/components/support/SupportChat.tsx, src/app/destek/page.tsx</files>
  <action>
    After Task 1 and a local smoke of `/destek` (dev server if available): fix remaining UX gaps — truncated stream recovery, 429/503 Turkish copy, WhatsApp CTA visibility after draft_order_summary, AI-off fallback clarity, reduced-motion safety. Do not expand into Phase 8 admin generator or order-status lookup (CONTEXT deferred).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Support UX polish complete and typechecks (SUPAI-03/04 mobile risk mitigation).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify Phase 7 success criteria</name>
  <files>src/components/support/SupportChat.tsx, src/app/destek/page.tsx</files>
  <what-built>
    CMS-grounded Turkish support / order helper on /destek: live FAQ/shipping answers, scoped refusal, WhatsApp draft handoff the user sends, KVKK-minimized client-only transcripts, premium OLED/glass AI Asistan UI, gated when AI off.
  </what-built>
  <action>
    Pause for human verification of Phase 7 ROADMAP success criteria and SUPAI-01…05 on /destek. Do not mark the phase complete until the resume signal is received. If issues are reported, fix them in follow-up commits before closing the plan.
  </action>
  <how-to-verify>
    1. With ANTHROPIC_API_KEY set and AI_FEATURES_ENABLED true: open /destek — confirm "AI Asistan" label; chat does not claim to be human; privacy/minimization note visible.
    2. Ask a kargo / ücretsiz kargo threshold question — answer must match current /admin/ayarlar freeShippingThreshold (or fallback site-config). Change the setting in admin (or confirm against displayed storefront footer/promo copy) and ask again in a new message — answer must track the live value (SUPAI-01).
    3. Ask an off-topic open-domain question (e.g. weather / politics) — expect a short Turkish redirect to paspas/sipariş scope, not an open-domain essay (SUPAI-02).
    4. Ask something the assistant cannot ground — expect a pre-filled WhatsApp handoff CTA; clicking opens wa.me with draft; AI does not send (SUPAI-03).
    5. Request an order summary with vehicle + colors + extras — review draft; price matches calculateMatPrice/configurator total; only user click sends via WhatsApp (SUPAI-04).
    6. Confirm no account/login is required to chat and no "history across visits" claim; refresh clears session messages (SUPAI-05).
    7. With AI disabled / key unset: /destek shows fallback links (FAQ/kargo/WhatsApp); no broken chat chrome.
    8. Visual: surface-glass/mac-glass, Racing Red, mobile layout OK; reduced-motion respected if enabled. Footer link reaches /destek.
  </how-to-verify>
  <verify>
    <automated>npm run ai:eval:support-chat</automated>
    <human-check>All how-to-verify steps pass on /destek</human-check>
  </verify>
  <done>Human approved Phase 7 success criteria (SUPAI-01…05 observable on /destek).</done>
  <resume-signal>Type "approved" or list issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Eval harness → grounding/pricing | Deterministic assertions; no live model trust for policy numbers |
| Human verify → production readiness | Catches grounding/refusal/UX gaps automation cannot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | grounding freshness eval | high | mitigate | Golden threshold-change suite required before phase complete |
| T-07-08 | Tampering | user-sends eval | high | mitigate | Source scan blocks auto-send / order write patterns |
| T-07-06 | Information disclosure | KVKK scan | medium | mitigate | Eval fails if transcript persistence APIs appear in support sources |
| T-07-05 | Spoofing | prompt disclosure eval | medium | mitigate | Prompt scan for AI Asistan / refusal rules |
</threat_model>

<verification>
- `npm run ai:eval:support-chat` passes
- `npm run typecheck` passes
- Optional: `npm run build` lists `/destek` and `/api/ai/support`
</verification>

<success_criteria>
- Automated gates cover grounding freshness, scoped refusal, user-sends, KVKK minimization
- Human confirms ROADMAP Phase 7 success criteria on /destek
</success_criteria>

<output>
Create `.planning/phases/07-support-order-helper-grounding/07-03-SUMMARY.md` when done
</output>
