---
phase: 09-storefront-boundaries-and-content-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/storefront-flags.ts
  - src/lib/storefront-flags.test.ts
  - .env.example
  - .planning/phases/09-storefront-boundaries-and-content-inventory/09-HOMEPAGE-INVENTORY.md
autonomous: true
requirements:
  - BOUND-01
must_haves:
  truths:
    - "Customer AI UI visibility is controlled by isCustomerAiUiEnabled() which defaults false unless CUSTOMER_AI_UI_ENABLED is true/1 AND isAiConfigured() is true"
    - "isAiFeaturesEnabled / isAiConfigured semantics are unchanged so admin AI and /api/ai routes stay independent of the customer UI flag"
    - "Homepage CMS section-key inventory exists as a reversible Phase 10 input without deleting Convex or seed rows"
  artifacts:
    - path: src/lib/storefront-flags.ts
      provides: "isCustomerAiUiEnabled() server-evaluated helper"
    - path: src/lib/storefront-flags.test.ts
      provides: "Vitest coverage for default-off and opt-in polarity"
    - path: .env.example
      provides: "CUSTOMER_AI_UI_ENABLED=false documented beside AI block"
    - path: .planning/phases/09-storefront-boundaries-and-content-inventory/09-HOMEPAGE-INVENTORY.md
      provides: "Mounted vs seeded sectionKey matrix for Phase 10"
  key_links:
    - from: src/lib/storefront-flags.ts
      to: src/lib/ai/config.ts
      via: "imports isAiConfigured; does not alter kill-switch polarity"
    - from: .env.example
      to: CUSTOMER_AI_UI_ENABLED
      via: "server-only; no NEXT_PUBLIC_ prefix"
---

<objective>
Add the dedicated customer AI UI visibility flag (BOUND-01 / D-02 / D-03) and document the homepage CMS mount inventory (D-07) for Phase 10.

Purpose: Split customer chrome visibility from the global AI capability kill switch so later plans can hide Destek/chat without disabling admin AI or API routes (D-01 / D-08).
Output: `isCustomerAiUiEnabled()` + tests + `.env.example` entry + `09-HOMEPAGE-INVENTORY.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-storefront-boundaries-and-content-inventory/09-CONTEXT.md
@.planning/phases/09-storefront-boundaries-and-content-inventory/09-RESEARCH.md
@.planning/phases/09-storefront-boundaries-and-content-inventory/09-PATTERNS.md
@src/lib/ai/config.ts
@src/lib/ai/config.test.ts
@src/app/page.tsx
@convex/cmsSeedData.ts
@.env.example

## Locked decision IDs (from 09-CONTEXT.md Locked Decisions)
- D-01: Keep customer AI implementations, API routes, evals, and admin AI in code
- D-02: Dedicated storefront UI flag (not solely the global kill switch)
- D-03: Customer UI flag defaults off
- D-07: Homepage CMS data and seed rows remain intact; inventory/mount decisions reversible
- D-08: Admin outside `.premium-site`; Convex fallback, pricing, cart, checkout untouched

## Executor hard warnings
- **Do not edit** `src/components/home/Hero.tsx` — user has an uncommitted WIP change; Phase 9 must not overwrite it.
- Do not modify `src/lib/ai/config.ts` kill-switch semantics, `/api/ai/**`, `/api/admin/ai/**`, or admin `/admin/icerik` AI gating.
- Do not delete Convex CMS rows or `CONTENT_SECTIONS_SEED` entries.
- No new npm dependencies.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Customer AI UI flag helper + tests + env docs</name>
  <files>src/lib/storefront-flags.ts, src/lib/storefront-flags.test.ts, .env.example</files>
  <read_first>
    - src/lib/ai/config.ts (isAiFeaturesEnabled defaults ON; isAiConfigured)
    - src/lib/ai/config.test.ts (vi.stubEnv / afterEach unstub pattern)
    - .env.example (AI block around ANTHROPIC_API_KEY / AI_FEATURES_ENABLED)
    - .planning/phases/09-storefront-boundaries-and-content-inventory/09-PATTERNS.md (storefront-flags pattern)
  </read_first>
  <behavior>
    - Unset / empty / false / 0 CUSTOMER_AI_UI_ENABLED → isCustomerAiUiEnabled() is false even with a valid Anthropic key and AI_FEATURES_ENABLED on
    - CUSTOMER_AI_UI_ENABLED true or 1 + valid key + features on → true
    - CUSTOMER_AI_UI_ENABLED true + missing/placeholder key → false
    - CUSTOMER_AI_UI_ENABLED true + AI_FEATURES_ENABLED false → false
    - isAiFeaturesEnabled / isAiConfigured behavior unchanged by this module
  </behavior>
  <action>
    Per D-02 and D-03 (agent discretion on location): create `src/lib/storefront-flags.ts` exporting `isCustomerAiUiEnabled()` that reads server-only `CUSTOMER_AI_UI_ENABLED` (trim + lowercase). Return true only when the value is `true` or `1` AND `isAiConfigured()` from `@/lib/ai/config` is true; otherwise false (default off). Do not use a NEXT_PUBLIC_ env name. Do not change `isAiFeaturesEnabled` polarity in config.ts.

    Add `src/lib/storefront-flags.test.ts` mirroring the Vitest harness in `config.test.ts` covering the behavior cases above.

    Document in `.env.example` beside the existing AI block: `CUSTOMER_AI_UI_ENABLED=false` with a short Turkish/English comment that this gates customer chrome only and does not disable admin AI or API capability when `AI_FEATURES_ENABLED` remains on (D-01).
  </action>
  <verify>
    <automated>npx vitest run src/lib/storefront-flags.test.ts src/lib/ai/config.test.ts</automated>
  </verify>
  <done>
    Helper defaults off; opt-in only via true/1 plus configured AI; existing config tests still pass; `.env.example` documents the flag.
  </done>
</task>

<task type="auto">
  <name>Task 2: Homepage CMS section inventory artifact</name>
  <files>.planning/phases/09-storefront-boundaries-and-content-inventory/09-HOMEPAGE-INVENTORY.md</files>
  <read_first>
    - src/app/page.tsx (mounted sections)
    - convex/cmsSeedData.ts (home CONTENT_SECTIONS_SEED keys)
    - src/lib/cms.ts or layout chrome keys if referenced by RESEARCH
    - .planning/phases/09-storefront-boundaries-and-content-inventory/09-RESEARCH.md (inventory tables)
  </read_first>
  <action>
    Per D-07: create `09-HOMEPAGE-INVENTORY.md` in this phase directory only (planning artifact, not production code). Columns at minimum: `sectionKey | mounted_on_home | consumer_component | keep_or_unmount_candidate | reversible_notes`.

    Include: currently mounted keys (`hero`, `hero-secondary-cta`, `featured`, `faq`, `faq-sidebar`), note that `HomeConfiguratorShowcase` is hardcoded (no CMS key), chrome keys (`chrome-header`, `chrome-footer`), seeded-but-unmounted home keys from RESEARCH (`steps` / `step-01..03`, `showcase*`, `testimonials`), and orphan home components (`FeatureStrip`, `LuxuryInterior`, `PremiumExperience`) as unmounted code inventory.

    Explicitly state Phase 10 owns actual unmounts; Phase 9 does not change `src/app/page.tsx` mounts. **Do not edit** `src/components/home/Hero.tsx`. Do not delete seed data or Convex rows.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/09-storefront-boundaries-and-content-inventory/09-HOMEPAGE-INVENTORY.md'; const t=fs.readFileSync(p,'utf8'); if(!/hero/.test(t)||!/mounted/.test(t)||!/step-01/.test(t)) process.exit(1); console.log('inventory ok')"</automated>
  </verify>
  <done>
    Inventory markdown exists with mounted vs seeded keys and Phase 10 handoff notes; no CMS/seed deletions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server env → storefront flag helper | Untrusted operators / mis-set env; flag is visibility only |
| Customer UI flag → API/admin capability | Must not be conflated; APIs stay on `isAiConfigured` / `isAiFeaturesEnabled` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Elevation of Privilege | Customer UI flag vs `/api/ai/*` | high | mitigate | UI flag never authorizes API; leave route guards on `isAiFeaturesEnabled` / `isAiConfigured` unchanged |
| T-09-02 | Tampering | Client-read feature flag | medium | mitigate | Server-only env (no NEXT_PUBLIC_); Header/Footer consume static spine / server-gated mounts in plan 02 |
| T-09-03 | Denial of Service | Accidental admin AI disable | medium | mitigate | Separate `CUSTOMER_AI_UI_ENABLED` from `AI_FEATURES_ENABLED`; document in `.env.example` |
| T-09-04 | Information Disclosure | Inventory docs leaking secrets | low | accept | Inventory lists sectionKeys only; no secrets |
| T-09-SC | Tampering | npm installs | high | mitigate | No new packages this plan — skip install |
</threat_model>

<verification>
- `npx vitest run src/lib/storefront-flags.test.ts src/lib/ai/config.test.ts` passes
- `09-HOMEPAGE-INVENTORY.md` present with mounted and orphan/seeded keys
- `git status` shows no modifications to `src/components/home/Hero.tsx` from this plan
</verification>

<success_criteria>
- BOUND-01 satisfied: dedicated customer UI flag defaults off without changing admin/API capability helpers
- D-07 inventory artifact ready for Phase 10 without CMS deletion
</success_criteria>

<output>
Create `.planning/phases/09-storefront-boundaries-and-content-inventory/09-01-SUMMARY.md` when done
</output>
