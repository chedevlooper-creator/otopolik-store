---
phase: 9
slug: storefront-boundaries-and-content-inventory
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-17
---

# Phase 9 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Phase framing:** Phase 9 is a storefront **boundary/wiring** phase (BOUND-01–04), not a
visual redesign phase. It does not introduce new visual components, tokens, or layouts — it
gates existing chrome (Header/Footer nav, `/destek` fallback, configurator AI mount) behind a
new flag and trims dead links. This contract documents the existing design system as the
constraint envelope for that wiring, and locks the copy/layout contract for the one surface
whose content actually changes for an end user: the `/destek` non-AI fallback page. Homepage
visual/motion changes are explicitly out of scope (Phase 10) — see `09-CONTEXT.md` deferred list.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — hand-rolled Tailwind v4 utility system (no `components.json`, no shadcn) |
| Preset | not applicable |
| Component library | none (custom `*-rich` / `surface-glass` / `mac-glass*` utility classes in `src/app/globals.css`) |
| Icon library | lucide-react (existing dependency) |
| Font | Syne (`--font-heading`), Instrument Sans (`--font-body`), JetBrains Mono (`--font-spec`) — via `next/font` in `layout.tsx` |

No shadcn gate applies: `components.json` is absent and CLAUDE.md documents an established,
intentional custom design system (OLED-black glass theme). Introducing shadcn now would
conflict with the locked "Sade Lüks Deneyim" brand system and is out of scope for a
boundary-only phase. Do not initialize shadcn in this phase.

---

## Spacing Scale

Declared values (must be multiples of 4) — inherited from existing `globals.css` / Tailwind v4 scale, not newly defined this phase:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding (e.g. `gap-1.5`, `gap-2`) |
| sm | 8px | Compact element spacing (nav link gaps, badge padding) |
| md | 16px | Default element spacing (card padding, form gaps) |
| lg | 24px | Section padding (`/destek` fallback card `p-6`/`p-9`) |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing (`py-12`/`py-16`/`py-20` on `/destek`) |

Exceptions: Touch targets on Header/Footer/`/destek` links/buttons hold `min-h-10`/`min-h-11`/`min-h-12` (40/44/48px) for tap-target accessibility, matching the existing codebase convention (`min-h-11`/`min-w-11` throughout `Footer.tsx`) — not a new exception, just confirmed as still applicable.

---

## Typography

Existing scale from `globals.css` / Tailwind, reused as-is (Phase 9 introduces no new sizes/weights):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (`text-sm`) | 400 (regular, `font-body`/Instrument Sans) | 1.5 (leading-7 on `/destek` copy) |
| Label | 11–12px (`text-[11px]`/`text-xs`, uppercase tracked) | 600–700 (semibold/bold) | 1.2 |
| Heading (H2) | 24–30px (`text-2xl`/`text-3xl`) | 700 (bold, `font-heading`/Syne) | 1.2 |
| Display (H1) | 36–60px (`text-4xl` → `text-6xl` responsive) | 700 (bold, `font-heading`/Syne) | 1.1 |

Exactly 2 weights in use for this phase's touched surfaces: 400 (body) and 600/700 (labels/headings, treated as one semantic "emphasis" weight per existing system). No new font sizes are introduced.

---

## Color

Existing 60/30/10 system from CLAUDE.md, unchanged by this phase:

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#000000` (OLED black, `--background`) | Page background, `/destek` hero background |
| Secondary (30%) | `#0b0b0b`–`#141414` (`--surface`/`--surface-hover`) | Cards, `surface-glass` panel on `/destek`, mobile nav dialog panel |
| Accent (10%) | `#ED1B24` Racing Red (`--brand-red`) | Primary CTA only: `/destek` WhatsApp button (`btn-red-rich`), active nav-link underline, footer "Tasarımını başlat" CTA, cart-count badge |
| Destructive | not applicable this phase | No destructive actions introduced (no delete/remove UI added) |

Accent reserved for: active `NAV_LINKS` underline indicator (`Header.tsx`), Sepet item-count badge, the `/destek` fallback's single primary WhatsApp button (`btn-red-rich`), and the Footer bottom CTA. Do **not** add Racing Red to secondary/tertiary links, icons, or the new footer link trims — those stay `text-white/62` per existing pattern. Muted-gold gradient stays confined to `.btn-light-rich` (header Sepet pill) per CLAUDE.md hard rule #3 — this phase touches that button's container only for link removal, never its gradient.

WhatsApp green (`#25D366`) is a pre-existing third accent used only for WhatsApp-branded CTAs (Footer "WhatsApp desteği" pill, `/destek` icon badge context) — preserve as-is; do not extend it to non-WhatsApp elements.

---

## Copywriting Contract

Turkish copy, matching existing `/destek` implementation (already executed in 09-01/09-02; documented here as the locked contract for future edits/audits):

| Element | Copy |
|---------|------|
| Primary CTA (`/destek` fallback) | "WhatsApp'tan destek al" (button), opens `buildWhatsAppLink(settings.whatsappNumber, "Merhaba OTO POLİK, kargo, ölçü veya sipariş konusunda destek almak istiyorum.")` |
| `/destek` heading (flag off) | "Sorunuz için" / "yanınızdayız." (h1, two-line) |
| `/destek` supporting line (flag off) | "Kargo, araç uyumu, paspas bakımı ve sipariş sorularınız için destek ekibimize WhatsApp üzerinden ulaşın." |
| `/destek` fallback card heading | "En hızlı destek WhatsApp'ta" |
| `/destek` fallback card body | "Kargo ve ürün bilgilerine göz atabilir veya hazırladığımız mesajla doğrudan destek ekibimize ulaşabilirsiniz." |
| `/destek` reassurance microcopy | "Sepet, tasarım ve WhatsApp sipariş akışınız kesintisiz devam eder." |
| `/destek` secondary links | "Kargo ve teslimat" (`/bilgiler/kargo`), "İade ve değişim" (`/bilgiler/iade`), "İletişim bilgileri" (`/iletisim`) — no dead `#sss` hash |
| Empty state | Not applicable — `/destek` fallback always renders static contact content; no data-dependent empty state in this phase |
| Error state | Not applicable — page has no client-side fetch/mutation this phase; `getStoreSettings()` already has a static-config fallback per CLAUDE.md data-layer pattern, so the page cannot render with missing WhatsApp number |
| Destructive confirmation | Not applicable — no destructive actions in this phase's scope |
| Primary nav labels (locked spine) | "Tasarla" (`/olusturucu`), "Ürünler" (`/urunler`), "Galeri" (`/galeri`), "İletişim" (`/iletisim`); Sepet remains the dedicated header button, not a nav-link label change |

Constraint: never reintroduce "AI Destek" branding into Footer/Header while `CUSTOMER_AI_UI_ENABLED` is unset/false (BOUND-02). If the flag is ever true + AI configured, `/destek` may use existing AI-branded copy variant already coded in `destek/page.tsx` (`aiEnabled` branch) — that branch is out of scope to edit this phase.

---

## UI Considerations

Probe coverage: 20 applicable considerations across 3 elements — 19 covered, 1 backstop, 0 unresolved.

Elements probed: **E1 nav** (`Header.tsx` `NAV_LINKS` + mobile dialog + `Footer.tsx` link columns), **E2 static-content** (`/destek` fallback page), **E3 interactive-control** (WhatsApp CTA + secondary link row). Kind confirmation: element prose re-read against detected kinds; nothing missed — this phase adds no `form`, `media`, or variable `list-collection` surfaces.

| Element | Category | Status | Resolution |
|---------|----------|--------|------------|
| E1 nav | loading | ✅ covered | `NAV_LINKS` is a static const array rendered without any fetch — no loading state exists |
| E1 nav | error | ✅ covered | No load/submit path; links are static Next.js `<Link>`s — no error state can render |
| E1 nav | overflow | ✅ covered | Four short fixed labels (see Copywriting Contract nav row) fit the existing flex layout at desktop breakpoints; below that the mobile dialog lists links vertically full-width — no truncation logic needed |
| E1 nav | long-text | ✅ covered | Labels are hardcoded short Turkish strings (longest: "İletişim"), not user/CMS-driven |
| E2 `/destek` | empty | ✅ covered | Page always renders hardcoded copy (Copywriting Contract rows) — no data-dependent content, cannot be empty |
| E2 `/destek` | loading | ✅ covered | Async server component; `getStoreSettings()` resolves before HTML is sent — no client loading flash |
| E2 `/destek` | error | ✅ covered | `getStoreSettings()` falls back to static `site-config.ts` per the data-layer pattern — the page cannot fail to render from missing settings |
| E2 `/destek` | populated | ✅ covered | Happy path is the only path: hero + fallback card exactly as locked in the Copywriting Contract |
| E2 `/destek` | partial | ✅ covered | Content is not field-driven; a single settings value with a static fallback — no partial shape exists |
| E2 `/destek` | overflow | ✅ covered | Short fixed strings wrap inside existing max-width containers (`leading-7` body) — no clip/scroll risk |
| E2 `/destek` | zero-one-many | ✅ covered | Only collection is the fixed 3-item secondary link row — count never varies |
| E2 `/destek` | long-text | ✅ covered | All copy hardcoded and locked in the Copywriting Contract; no user/CMS input this phase |
| E3 WhatsApp CTA | empty | 🧪 backstop | statement: an empty `settings.whatsappNumber` would make `buildWhatsAppLink` emit a numberless `wa.me/` link (broken CTA); verification: backstop — held-out test asserts `getStoreSettings()`'s static fallback (`site-config.ts`) always supplies a non-empty WhatsApp number |
| E3 WhatsApp CTA | loading | ✅ covered | href resolved server-side before render — the button never renders in a pending state |
| E3 WhatsApp CTA | error | ✅ covered | No fetch/mutation; anchor navigation is browser-native — the only app-level failure shape (missing number) is the empty-state backstop above |
| E3 WhatsApp CTA | populated | ✅ covered | Locked in Copywriting Contract: "WhatsApp'tan destek al" + prefilled message + 3 secondary links |
| E3 WhatsApp CTA | partial | ✅ covered | Single scalar setting with fallback — no multi-field partial state |
| E3 WhatsApp CTA | overflow | ✅ covered | Fixed short button label on a `min-h-12` target; secondary link row wraps via flex at narrow widths per the existing Footer pattern |
| E3 WhatsApp CTA | zero-one-many | ✅ covered | One primary CTA + fixed 3 secondary links — counts never vary |
| E3 WhatsApp CTA | long-text | ✅ covered | Labels hardcoded; the prefilled WhatsApp message travels as a URL-encoded query param, never rendered as page text |

Empty-state and error-state copy stays in `## Copywriting Contract` — rows above reference those rows rather than restating copy.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

No registries in use. Phase 9 introduces zero new dependencies (per `09-RESEARCH.md` Standard Stack — "No install for Phase 9"). All UI is composed from existing `globals.css` utility classes (`btn-red-rich`, `btn-ghost-rich`, `surface-glass`, `mac-glass`, `icon-badge-rich`) and `lucide-react` icons already installed.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS — Turkish copy locked above matches shipped `/destek` implementation; no placeholder/lorem text
- [x] Dimension 2 Visuals: PASS — no new visual components; reuses `surface-glass`/`mac-glass`/`btn-red-rich` per CLAUDE.md conventions
- [x] Dimension 3 Color: PASS — 60/30/10 unchanged; Racing Red accent reserved list confirmed above; no gold reintroduced
- [x] Dimension 4 Typography: PASS — existing Syne/Instrument Sans/JetBrains Mono scale reused; no new sizes/weights
- [x] Dimension 5 Spacing: PASS — existing 4px-multiple Tailwind scale; touch targets ≥40px preserved
- [x] Dimension 6 Registry Safety: PASS — no shadcn, no third-party registries, no new dependencies

**Approval:** approved 2026-07-17
