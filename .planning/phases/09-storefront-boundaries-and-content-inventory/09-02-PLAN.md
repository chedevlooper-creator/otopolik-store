---
phase: 09-storefront-boundaries-and-content-inventory
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/components/Header.tsx
  - src/components/Footer.tsx
  - src/app/destek/page.tsx
  - src/app/olusturucu/page.tsx
autonomous: true
requirements:
  - BOUND-02
  - BOUND-03
  - BOUND-04
must_haves:
  truths:
    - "Primary header NAV_LINKS are only Tasarla, Ürünler, Galeri, İletişim; Sepet remains the existing header cart control; no hash hrefs in primary nav"
    - "With customer AI UI flag off (default), header/footer/configurator/support chrome expose no AI chat mounts and no AI-branded Destek entry points"
    - "Visiting /destek with flag off shows WhatsApp/contact fallback using getStoreSettings + buildWhatsAppLink with no SupportChat UI"
    - "/olusturucu does not mount ConfiguratorChat or pass aiEnabled true unless isCustomerAiUiEnabled() is true"
  artifacts:
    - path: src/components/Header.tsx
      provides: "Spine NAV_LINKS without Destek or hash anchors"
    - path: src/components/Footer.tsx
      provides: "Plain Destek or omit; no AI-branded Destek; no dead hash anchors (/#renkler, /#ozellikler, /#sss)"
    - path: src/app/destek/page.tsx
      provides: "Non-AI WhatsApp/contact fallback when UI flag off"
    - path: src/app/olusturucu/page.tsx
      provides: "Customer AI mounts gated by isCustomerAiUiEnabled()"
  key_links:
    - from: src/app/destek/page.tsx
      to: src/lib/storefront-flags.ts
      via: "isCustomerAiUiEnabled() selects chat vs WhatsApp fallback"
    - from: src/app/olusturucu/page.tsx
      to: src/lib/storefront-flags.ts
      via: "aiEnabled = isCustomerAiUiEnabled() → MatConfigurator + optional chat"
    - from: src/app/destek/page.tsx
      to: src/lib/whatsapp.ts
      via: "buildWhatsAppLink(settings.whatsappNumber, …)"
---

<objective>
Apply storefront chrome and route boundaries: slim primary navigation (BOUND-04 / D-06), remove AI-branded support entry points (BOUND-02 / D-04), replace `/destek` chat with WhatsApp/contact fallback when the customer UI flag is off (BOUND-03 / D-05), and gate configurator AI mounts with `isCustomerAiUiEnabled()` from plan 01.

Purpose: Lock reversible presentation boundaries so Phase 10+ deletion cannot resurrect customer AI chrome or dead nav.
Rationale: Static customer chrome uses fixed non-AI navigation, so no unused layout/provider flag wiring is introduced.
Output: Updated Header/Footer link constants + `/destek` fallback + `/olusturucu` mount gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-storefront-boundaries-and-content-inventory/09-CONTEXT.md
@.planning/phases/09-storefront-boundaries-and-content-inventory/09-RESEARCH.md
@.planning/phases/09-storefront-boundaries-and-content-inventory/09-PATTERNS.md
@.planning/phases/09-storefront-boundaries-and-content-inventory/09-01-PLAN.md
@src/lib/storefront-flags.ts
@src/components/Header.tsx
@src/components/Footer.tsx
@src/app/destek/page.tsx
@src/app/olusturucu/page.tsx
@src/lib/whatsapp.ts
@src/app/iletisim/page.tsx

## Locked decision IDs
- D-01: Keep AI implementations/APIs/admin in code (do not delete SupportChat / ConfiguratorChat / VehicleMatchInput / API routes)
- D-04: With flag off, header/footer/configurator/support chrome show no AI or AI Destek entry points
- D-05: `/destek` remains a valid route as static WhatsApp/contact fallback (not 404)
- D-06: Primary nav = Tasarla, Ürünler, Galeri, İletişim, Sepet; no dead hash anchors
- D-08: Do not touch pricing, cart, checkout, Convex schema, or admin AI gates

## Executor hard warnings
- **Do not edit** `src/components/home/Hero.tsx` — preserve the user's uncommitted change; this plan never needs that file.
- Do not change `src/lib/ai/config.ts`, admin `isAiConfigured()` usage, or `/api/ai/**` handlers.
- Do not hardcode a WhatsApp phone number — use `getStoreSettings().whatsappNumber` + `buildWhatsAppLink`.
- Prefer not editing `MatConfigurator.tsx` / `VehicleSelector.tsx` if `olusturucu/page.tsx` can pass the new boolean alone.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slim Header spine and Footer AI/dead-hash links</name>
  <files>src/components/Header.tsx, src/components/Footer.tsx</files>
  <read_first>
    - src/components/Header.tsx (NAV_LINKS + Sepet openDrawer button)
    - src/components/Footer.tsx (KURUMSAL_LINKS, URUNLER_LINKS, WhatsApp CTA)
    - .planning/phases/09-storefront-boundaries-and-content-inventory/09-PATTERNS.md (Header/Footer sections)
  </read_first>
  <action>
    Per D-06 / BOUND-04: replace Header `NAV_LINKS` with exactly four path links in conversion order — `/olusturucu` Tasarla, `/urunler` Ürünler, `/galeri` Galeri, `/iletisim` İletişim. Remove Ana Sayfa, Özellikler hash, Destek, and Hakkımızda from primary nav. Keep the existing Sepet drawer button as the fifth spine affordance (do not remove it). Preserve `isNavLinkActive`, mobile dialog a11y, search/phone/CMS chrome wiring.

    Per D-04 / BOUND-02 and resolved Open Questions: in Footer, prefer a plain Destek label pointing at `/destek` for contact discoverability (or omit Destek from footer link groups entirely). Never advertise AI support in chrome. Remove all dead homepage hash-anchor links from footer product/corporate lists (colors, features, and FAQ fragments — the three `/#…` hrefs called out in Task verify). Replace needed destinations with real routes such as `/iletisim` or `/bilgiler/kargo`. Keep legal `/bilgiler/*`, WhatsApp float/CTA via settings, and secondary destinations like Hakkımızda if desired.

    Rationale: static customer chrome uses fixed non-AI navigation — do not add layout.tsx or StorefrontFlagsProvider wiring for Header/Footer; server `isCustomerAiUiEnabled()` gates belong only on AI mount routes in Task 2. Do not read server env from these client components.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('src/components/Header.tsx','utf8'); const m=h.match(/const NAV_LINKS = \[([\s\S]*?)\];/); if(!m) process.exit(1); const body=m[1]; const need=['/olusturucu','/urunler','/galeri','/iletisim']; for (const n of need) if(!body.includes(n)) {console.error('missing',n); process.exit(1);} if(body.includes('/destek')||body.includes('/#')||body.includes('Ana Sayfa')||body.includes('Hakk')) {console.error('spine pollution'); process.exit(1);} const f=fs.readFileSync('src/components/Footer.tsx','utf8'); if(/AI Destek/.test(f)) {console.error('footer AI label'); process.exit(1);} for (const hash of ['/#renkler','/#ozellikler','/#sss']) { if(f.includes(hash)||body.includes(hash)) {console.error('dead hash',hash); process.exit(1);} } console.log('chrome ok')"</automated>
  </verify>
  <done>
    Header primary links match the locked spine; Sepet control remains; Footer has no AI-branded Destek label and no dead hash links (`/#renkler`, `/#ozellikler`, `/#sss`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Gate /destek fallback and /olusturucu AI mounts</name>
  <files>src/app/destek/page.tsx, src/app/olusturucu/page.tsx</files>
  <read_first>
    - src/app/destek/page.tsx (full file — current isAiConfigured branch + SupportChat import)
    - src/app/olusturucu/page.tsx (aiEnabled = isAiConfigured)
    - src/lib/storefront-flags.ts (from plan 01)
    - src/app/iletisim/page.tsx (contact channel patterns)
    - src/lib/whatsapp.ts
  </read_first>
  <action>
    Per D-05 / BOUND-03: rewrite `src/app/destek/page.tsx` so the default path (when `!isCustomerAiUiEnabled()`) is a contact-first WhatsApp/support page — Turkish metadata such as Destek ve İletişim (not AI-assistant framing), primary CTA via `getStoreSettings()` + `buildWhatsAppLink`, secondary links to real routes (`/iletisim`, `/bilgiler/kargo`; avoid `/#sss` hash). Reuse `surface-glass` / `mac-glass` / `btn-red-rich` / `btn-ghost-rich`. Prefer MessageCircle (or similar non-bot) iconography on the fallback.

    Critical for BOUND-02 / pitfall avoidance: do not statically import `SupportChat` on the default-off path. Use a conditional dynamic import (or split so the chat module is only loaded when `isCustomerAiUiEnabled()` is true) so the chat client island is not pulled for the fallback. When the flag is on, existing SupportChat UX may remain for FUT-05 re-enablement (D-01 keep-in-code). Route must stay `/destek` (no `notFound()`).

    Per D-04 / BOUND-02: in `src/app/olusturucu/page.tsx`, replace `isAiConfigured()` mount gate with `isCustomerAiUiEnabled()` from `@/lib/storefront-flags` for both ConfiguratorChat conditional render and `MatConfigurator aiEnabled={…}`. Prefer dynamic import of ConfiguratorChat on the enabled branch when practical; at minimum do not render chat when the flag is off. Do not delete chat components or change admin/API gates. Leave MatConfigurator/VehicleSelector files unchanged if the prop alone hides VehicleMatchInput.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const d=fs.readFileSync('src/app/destek/page.tsx','utf8'); if(!d.includes('isCustomerAiUiEnabled')) {console.error('destek missing flag'); process.exit(1);} if(!d.includes('buildWhatsAppLink')) {console.error('destek missing wa'); process.exit(1);} if(/import SupportChat from/.test(d)) {console.error('static SupportChat import'); process.exit(1);} if(d.includes('/#sss')) {console.error('hash sss'); process.exit(1);} const o=fs.readFileSync('src/app/olusturucu/page.tsx','utf8'); if(!o.includes('isCustomerAiUiEnabled')) {console.error('olusturucu missing flag'); process.exit(1);} if(/isAiConfigured\(/.test(o)) {console.error('olusturucu still uses isAiConfigured'); process.exit(1);} console.log('routes ok')"</automated>
  </verify>
  <done>
    `/destek` defaults to WhatsApp/contact without static SupportChat import; `/olusturucu` mounts customer AI only via `isCustomerAiUiEnabled()`; chat component files remain in the repo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server page → client chat island | Mounting SupportChat/ConfiguratorChat ships AI client code |
| Storefront chrome → support intent | Nav/footer links steer users into AI vs WhatsApp paths |
| Settings → WhatsApp URL | Phone number from store settings must not be hardcoded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Information Disclosure | `/destek` SupportChat | medium | mitigate | No static SupportChat import when UI flag off; contact-first fallback only |
| T-09-06 | Elevation of Privilege | UI hide vs API | high | mitigate | Do not weaken `/api/ai/*`; only change page mounts to `isCustomerAiUiEnabled()` |
| T-09-07 | Spoofing | Hardcoded WhatsApp number | low | mitigate | Use `getStoreSettings().whatsappNumber` + `buildWhatsAppLink` only |
| T-09-08 | Tampering | Client Header inventing Destek | medium | mitigate | Remove Destek from NAV_LINKS permanently for v1.2 spine (BOUND-04); flag does not need client env |
| T-09-SC | Tampering | npm installs | high | mitigate | No new packages |
</threat_model>

<verification>
- Automated node asserts on Header/Footer/destek/olusturucu pass
- `npx vitest run src/lib/storefront-flags.test.ts` still passes
- `npm run typecheck` passes for touched routes (or project typecheck if cheap)
- Confirm `git status` / diff does not include `src/components/home/Hero.tsx`
</verification>

<success_criteria>
- BOUND-02: no AI Destek chrome; no customer chat mounts when flag off
- BOUND-03: `/destek` WhatsApp/contact fallback without chat UI when flag off
- BOUND-04: primary nav spine only; Sepet affordance kept; no primary hash anchors
</success_criteria>

<output>
Create `.planning/phases/09-storefront-boundaries-and-content-inventory/09-02-SUMMARY.md` when done
</output>
