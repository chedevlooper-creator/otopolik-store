---
phase: 06-streaming-chat-configurator-assistant
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - src/lib/ai/evals/configurator-chat-golden.ts
  - src/lib/ai/evals/configurator-chat-eval.test.ts
  - package.json
  - src/components/configurator/ConfiguratorChat.tsx
autonomous: false
requirements:
  - CFGAI-01
  - CFGAI-03
  - CFGAI-05
must_haves:
  truths:
    - "Automated eval asserts cart/tool prices equal calculateMatPrice for golden configurator configs (CFGAI-03)"
    - "Eval cases cover Turkish flow vocabulary (taban, kenar, topuk, bagaj) and AI Asistan disclosure expectations (CFGAI-01/05)"
    - "npm script ai:eval:configurator-chat gates regressions without requiring ANTHROPIC_API_KEY for price assertions"
    - "Human confirms streaming chat + stepper + cart + WhatsApp + AI-off fallback on /olusturucu"
  artifacts:
    - path: src/lib/ai/evals/configurator-chat-golden.ts
      provides: "Golden cases for price equality + Turkish assistant constraints"
    - path: src/lib/ai/evals/configurator-chat-eval.test.ts
      provides: "Vitest runner for configurator chat evals"
  key_links:
    - from: "buildCartItem / getCustomerMatPrice"
      to: "calculateMatPrice"
      via: "eval price-equality assertions"
    - from: "package.json ai:eval:configurator-chat"
      to: "configurator-chat-eval.test.ts"
      via: "vitest run"
---

<objective>
Lock CFGAI-03 price integrity and CFGAI-01/05 behavioral guardrails with a golden eval harness, polish any streaming reconnect gaps found during verification, and human-check the /olusturucu experience.

Purpose: Prevent price hallucination regressions (Pitfall 1) and confirm phase success criteria before Phase 7 reuses the chat pattern.
Output: Golden eval module + npm script, optional UX polish, human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-streaming-chat-configurator-assistant/06-CONTEXT.md
@.planning/phases/05-ai-infrastructure-vehicle-matcher/05-03-SUMMARY.md
@src/lib/ai/evals/vehicle-match-golden.ts
@src/lib/ai/evals/vehicle-match-eval.test.ts
@src/lib/ai/customer-tools.ts
@src/lib/mat-pricing.ts
@src/components/configurator/ConfiguratorChat.tsx
@package.json
</context>

<decision_map>
- D-03: Price-equality evals mandatory (never trust model-stated numbers)
- D-06: Disclosure / AI Asistan constraints reflected in golden expectations where testable
Discretion: Keep evals deterministic without live Anthropic calls for price/cart assertions (mirror vehicle-match eval pattern). Live streaming remains human-verified.
</decision_map>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Configurator chat golden eval + npm script</name>
  <files>src/lib/ai/evals/configurator-chat-golden.ts, src/lib/ai/evals/configurator-chat-eval.test.ts, package.json</files>
  <behavior>
    - For each golden config { heelPad, trunkMat }, getCustomerMatPrice(options) === calculateMatPrice(options)
    - buildCartItem-equivalent helper (or exported pure function from provider/tools) yields price matching calculateMatPrice; never accepts a model-supplied override price
    - Isolation: configurator-tools and chat route sources still contain no requireAdminKey / admin mutation imports
    - At least one case documents expected Turkish step vocabulary / AI Asistan disclosure string presence in prompt module
    - Suite passes with ANTHROPIC_API_KEY unset
  </behavior>
  <action>
    Per D-03: Mirror Phase 5 eval layout. Add `configurator-chat-golden.ts` with ≥8 cases covering extras combinations and representative vehicle+color configs used for cart slug/summary shape checks. Add `configurator-chat-eval.test.ts` that asserts price equality and scans prompt/tools for AI Asistan + no-price-invention instructions (CFGAI-05 / Pitfall 1).

    If Plan 02 kept buildCartItem inside a client component, extract a pure `buildConfiguredMatCartItem(...)` helper under `src/lib/` (e.g. `src/lib/configurator-cart-item.ts`) so evals can import it without React — same function the provider uses.

    Add package.json script `ai:eval:configurator-chat` running vitest on the new eval file (alongside existing `ai:eval:vehicle-match`).
  </action>
  <verify>
    <automated>npm run ai:eval:configurator-chat</automated>
  </verify>
  <done>Golden eval passes without API key; price-equality and disclosure/isolation checks green (CFGAI-03/05).</done>
</task>

<task type="auto">
  <name>Task 2: Streaming reconnect polish from eval/smoke gaps</name>
  <files>src/components/configurator/ConfiguratorChat.tsx</files>
  <action>
    After Task 1 and a local smoke of `/olusturucu` (dev server if available): fix any remaining stream-interrupt UX gaps — ensure truncated mid-sentence bubbles cannot remain as the final state, Yeniden dene works after abort, and 429/503 copy stays Turkish. Do not expand into Phase 7 support grounding or Convex transcripts.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Stream error/retry path is complete and typechecks (CFGAI-01 mobile risk mitigation).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify Phase 6 success criteria</name>
  <files>src/components/configurator/ConfiguratorChat.tsx, src/app/olusturucu/page.tsx</files>
  <what-built>
    Streaming AI Asistan on /olusturucu that drives MatConfigurator, prices via calculateMatPrice, adds to real cart, WhatsApp handoff, premium OLED/glass UI, gated when AI off.
  </what-built>
  <action>
    Pause for human verification of Phase 6 ROADMAP success criteria and CFGAI-01…05 on /olusturucu. Do not mark the phase complete until the resume signal is received. If issues are reported, fix them in follow-up commits before closing the plan.
  </action>
  <how-to-verify>
    1. With ANTHROPIC_API_KEY set and AI_FEATURES_ENABLED true: open /olusturucu — confirm "AI Asistan" label visible; chat does not claim to be human.
    2. Chat in Turkish through vehicle → taban → kenar → ekstralar; confirm the real stepper progress/state updates (not a separate fake flow).
    3. Ask for price — number must match the configurator total (calculateMatPrice). Add to cart — cart line price matches.
    4. Use WhatsApp handoff CTA — wa.me opens with draft; user sends (AI does not place order).
    5. On mobile or DevTools Slow 4G: interrupt a stream (offline toggle) — truncated bubble recovers via error + Yeniden dene.
    6. With AI disabled / key unset: chat hidden; manual MatConfigurator still fully usable.
    7. Visual: surface-glass/mac-glass, Racing Red, no white bolted-on chat chrome; reduced-motion respected if enabled.
  </how-to-verify>
  <verify>
    <automated>npm run ai:eval:configurator-chat</automated>
    <human-check>All how-to-verify steps pass on /olusturucu</human-check>
  </verify>
  <done>Human approved Phase 6 success criteria (CFGAI-01…05 observable on /olusturucu).</done>
  <resume-signal>Type "approved" or list issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Eval harness → pricing functions | Deterministic assertions; no live model trust for prices |
| Human verify → production readiness | Catches UX/stream gaps automation cannot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | price evals | high | mitigate | Golden price-equality suite required before phase complete |
| T-06-04 | Spoofing | prompt disclosure | medium | mitigate | Eval scans prompt for AI Asistan / anti-impersonation rules |
| T-06-08 | Denial of service | reconnect polish | medium | mitigate | Human + code verify bounded retry UX |
</threat_model>

<verification>
- `npm run ai:eval:configurator-chat` passes
- `npm test -- src/lib/ai` still passes (no Phase 5 regressions)
- `npm run typecheck` and `npm run build` pass
- Human checkpoint approved
</verification>

<success_criteria>
- Phase success criteria 1–4 from ROADMAP Phase 6 are met
- CFGAI-01…05 covered with automated and human evidence
</success_criteria>

<output>
Create `.planning/phases/06-streaming-chat-configurator-assistant/06-03-SUMMARY.md` when done
</output>
