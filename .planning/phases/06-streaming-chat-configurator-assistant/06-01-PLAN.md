---
phase: 06-streaming-chat-configurator-assistant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/config.ts
  - src/lib/ai/config.test.ts
  - src/lib/ai/configurator-tools.ts
  - src/lib/ai/configurator-tools.test.ts
  - src/lib/ai/configurator-prompt.ts
  - src/app/api/ai/chat/route.ts
  - package.json
autonomous: true
requirements:
  - CFGAI-01
  - CFGAI-02
  - CFGAI-03
must_haves:
  truths:
    - "POST /api/ai/chat streams Turkish assistant tokens via Vercel AI SDK streamText when AI is configured"
    - "Chat tools call runVehicleMatch / getCustomerMatPrice / getCustomerVehiclePrice — never invent prices (CFGAI-03, AIINF-03)"
    - "Chat tool module has zero adminKey / requireAdminKey / Convex mutation imports (AIINF-05)"
    - "Unset key or AI_FEATURES_ENABLED=false yields 503 ai_unavailable; rate limit returns 429 (AIINF-02/04 carryover)"
  artifacts:
    - path: src/app/api/ai/chat/route.ts
      provides: "Streaming configurator chat route (SSE UI message stream)"
    - path: src/lib/ai/configurator-tools.ts
      provides: "Server-executable + client-intent tool schemas for stepper/price/vehicle"
    - path: src/lib/ai/configurator-prompt.ts
      provides: "Turkish system prompt: AI Asistan, no price invention, drive real stepper"
    - path: src/lib/ai/config.ts
      provides: "configurator-chat AiFeature with Sonnet model + token cap"
  key_links:
    - from: "streamText tools"
      to: "runVehicleMatch + customer-tools price helpers"
      via: "src/lib/ai/configurator-tools.ts"
    - from: "getLanguageModel('configurator-chat')"
      to: "getAnthropicProvider() lazy null"
      via: "src/lib/ai/anthropic-client.ts"
---

<objective>
Stand up the streaming `/api/ai/chat` route, Turkish configurator system prompt, and catalog-grounded tools that Phase 6 UI will consume — reusing Phase 5 AI client, rate limit, kill switch, `runVehicleMatch`, and customer price helpers.

Purpose: Satisfy CFGAI-01 (server streaming path), CFGAI-02 (tool surface that can drive the real stepper), CFGAI-03 (prices only from `calculateMatPrice` / `getVehiclePrice` wrappers) before any chat UI exists.
Output: Extended `AiFeature`, `configurator-tools` + prompt modules, `POST /api/ai/chat` with streamText, vitest coverage for tools/guardrails.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-streaming-chat-configurator-assistant/06-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/05-ai-infrastructure-vehicle-matcher/05-SUMMARY.md
@src/lib/ai/config.ts
@src/lib/ai/anthropic-client.ts
@src/lib/ai/customer-tools.ts
@src/lib/ai/rate-limit.ts
@src/lib/ai/vehicle-match.ts
@src/app/api/ai/vehicle-match/route.ts
@src/lib/mat-pricing.ts
@CLAUDE.md
</context>

<decision_map>
CONTEXT Implementation Decisions (assign D-IDs for coverage; honor exactly):
- D-01: streamText over `src/app/api/ai/chat`; reuse Phase 5 server-only client; conversational Sonnet (not Haiku matcher model)
- D-02: Tools expose real MatConfigurator steps (vehicle, floor, edge, extras) — no parallel fictional checkout
- D-03: LLM never composes prices; always `getCustomerMatPrice` / `getCustomerVehiclePrice` → `calculateMatPrice` / `getVehiclePrice`
- D-04: Chat tools isolated from admin-key mutations (AIINF-05)
- D-07: Kill switch / unconfigured → 503; no throw on missing key
- D-08: WhatsApp handoff tool prepares wa.me draft only (user sends) — full UI open in Plan 02
Discretion locked here: no Convex chatSessions/chatMessages this phase (CONTEXT deferred). Client-applied UI tools declared without server execute; price/vehicle tools execute server-side.
</decision_map>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend AI config + configurator tool contracts</name>
  <files>src/lib/ai/config.ts, src/lib/ai/config.test.ts, src/lib/ai/configurator-tools.ts, src/lib/ai/configurator-tools.test.ts, src/lib/ai/configurator-prompt.ts</files>
  <behavior>
    - AiFeature includes configurator-chat with a pinned Sonnet model id and max_tokens ≥ 1024 (higher than vehicle-match 256)
    - getCustomerMatPrice({ heelPad, trunkMat }) equals calculateMatPrice for the same options (price-equality)
    - Tool handlers for match_vehicle call runVehicleMatch(query) and return its structured status — never invent brand/model
    - configurator-tools.ts source fails isolation checks if it imports adminKey, requireAdminKey, or Convex mutation helpers
    - Palette setters reject color names outside the MatConfigurator FLOOR_COLORS / EDGE_COLORS name sets (shared constant export or duplicated name allowlist kept in sync)
  </behavior>
  <action>
    Per D-01/D-03/D-04: Extend `AiFeature` in `src/lib/ai/config.ts` with `"configurator-chat"`. Pin a dated Anthropic Sonnet snapshot for conversational chat (research/CONTEXT name `claude-sonnet-5` — verify the exact API model string against `@ai-sdk/anthropic` / Anthropic docs at implement time; do not reuse `claude-haiku-4-5-20251001`). Set a chat-appropriate `AI_MAX_TOKENS` (at least 1024). Update `config.test.ts` for the new feature.

    Create `src/lib/ai/configurator-tools.ts` using Vercel AI SDK `tool` + zod schemas. Server-executable tools:
    - `match_vehicle` → `runVehicleMatch` from `src/lib/ai/vehicle-match.ts` (reuse Phase 5; do not reimplement search)
    - `get_mat_price` → `getCustomerMatPrice` from `src/lib/ai/customer-tools.ts`
    - `get_vehicle_price` → `getCustomerVehiclePrice` from the same module

    Client-intent tools (schema only / no privileged execute on server — Plan 02 applies them to React state):
    - `set_vehicle` (brand, model, optional year/body)
    - `set_floor_color` / `set_edge_color` (Turkish color names matching MatConfigurator palettes)
    - `set_extras` (heelPad, trunkMat booleans)
    - `add_to_cart` (signals client to call useCart().addItem with calculateMatPrice — server must NOT write cart)
    - `prepare_whatsapp_handoff` (returns draft message fields; does not place an order)

    Create `src/lib/ai/configurator-prompt.ts` exporting a Turkish system prompt that: (1) identifies as AI Asistan (CFGAI-05), (2) forbids inventing fiyat/indirim/kargo numbers (D-03), (3) requires tool calls to advance vehicle → taban → kenar → ekstralar → fiyat → WhatsApp (CFGAI-01/02), (4) never claims to be a human rep.

    Write `configurator-tools.test.ts` covering price-equality, vehicle-match delegation, palette rejection, and admin-isolation source scan (mirror `customer-tools.test.ts`). Export floor/edge color name lists from a shared module if needed so MatConfigurator and tools stay in sync — prefer extracting palette constants from `MatConfigurator.tsx` into e.g. `src/lib/mat-colors.ts` rather than drifting copies.
  </action>
  <verify>
    <automated>npm test -- src/lib/ai/config.test.ts src/lib/ai/configurator-tools.test.ts</automated>
  </verify>
  <done>configurator-chat feature configured; tools + prompt exist; price-equality and isolation tests pass (CFGAI-02/03, D-01/D-03/D-04).</done>
</task>

<task type="auto">
  <name>Task 2: Ship streaming POST /api/ai/chat route</name>
  <files>src/app/api/ai/chat/route.ts</files>
  <action>
    Per D-01/D-07: Create `src/app/api/ai/chat/route.ts` with `export const runtime = "nodejs"`. Mirror guardrails from `src/app/api/ai/vehicle-match/route.ts`:
    - If `!isAiFeaturesEnabled()` → JSON 503 `{ error: "ai_unavailable" }`
    - If `!isAiConfigured()` or `getLanguageModel("configurator-chat")` is null → same 503 (graceful; never throw on missing key)
    - Parse JSON body with UI messages (AI SDK chat protocol). Reject empty/oversized payloads with Turkish error codes (keep messages bounded — e.g. max ~20 turns or truncate older content; max input chars per user message ~2000)
    - `checkAiRateLimit(getClientKey(request))` with the same IP + optional `x-ai-session` key pattern as vehicle-match; on deny return 429 + Retry-After
    - Call `streamText` from `ai` with model from `getLanguageModel("configurator-chat")`, system/instructions from configurator-prompt, tools from configurator-tools, `maxTokens` via `getAiMaxTokens("configurator-chat")`, and `stopWhen` / step limits appropriate for multi-tool turns
    - Return `result.toUIMessageStreamResponse()` (AI SDK v7). Do not put ANTHROPIC_API_KEY in any response header or body.
    - On unexpected errors: log server-side, return a non-leaking 500 JSON or stream error part — never stack traces to client.

    Do not create Convex transcript tables (deferred). Do not import admin auth.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | Select-String -Pattern "api/ai/chat|configurator-" ; if (-not $?) { npm run typecheck }</automated>
  </verify>
  <done>Chat route streams via streamText with Phase 5 guardrails; unconfigured/kill-switch/rate-limit paths match vehicle-match semantics (CFGAI-01 server path, D-01/D-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/ai/chat | Untrusted Turkish free-text + message history |
| Chat tools → pricing/catalog | Must stay read-only / intent-only; no admin or order writes |
| Stream → client | Model text must not be treated as authoritative price |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering / Elevation | configurator-tools | high | mitigate | No adminKey/Convex mutation imports; client cart writes only via Plan 02 useCart |
| T-06-02 | Information disclosure | chat route | medium | mitigate | Server-only key; isAiConfigured gate; no key in JSON/SSE |
| T-06-03 | Denial of service | chat route rate limit | high | mitigate | Reuse checkAiRateLimit + max tokens + message size caps |
| T-06-04 | Spoofing | system prompt / UI copy | medium | mitigate | Prompt forces AI Asistan identity; Plan 02 UI label (CFGAI-05) |
| T-06-05 | Tampering | get_mat_price tool | high | mitigate | Prices only via getCustomerMatPrice → calculateMatPrice; eval in Plan 03 |
| T-06-SC | Tampering | npm @ai-sdk/react (Plan 02) | low | accept | Official Vercel package same family as installed `ai@7` / `@ai-sdk/anthropic@4` — verify on npm at install |
</threat_model>

<verification>
- `npm test -- src/lib/ai/config.test.ts src/lib/ai/configurator-tools.test.ts` passes
- `npm run typecheck` passes for new AI chat files
- Focused lint on touched `src/lib/ai/**` and `src/app/api/ai/chat/**` (ignore pre-existing app lint debt)
</verification>

<success_criteria>
- Streaming chat API exists and reuses Phase 5 client/rate-limit/kill-switch
- Tools can match vehicles and return prices equal to calculateMatPrice
- No admin mutation surface reachable from chat tools
</success_criteria>

<output>
Create `.planning/phases/06-streaming-chat-configurator-assistant/06-01-SUMMARY.md` when done
</output>
