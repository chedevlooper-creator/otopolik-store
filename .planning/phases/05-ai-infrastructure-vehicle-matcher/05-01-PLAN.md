---
phase: 05-ai-infrastructure-vehicle-matcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .env.example
  - src/lib/ai/anthropic-client.ts
  - src/lib/ai/config.ts
  - src/lib/ai/rate-limit.ts
  - src/lib/ai/customer-tools.ts
  - src/lib/ai/config.test.ts
  - src/lib/ai/rate-limit.test.ts
  - src/lib/ai/customer-tools.test.ts
autonomous: true
requirements:
  - AIINF-01
  - AIINF-02
  - AIINF-03
  - AIINF-04
  - AIINF-05
must_haves:
  truths:
    - "Server-only AI helpers detect missing/placeholder ANTHROPIC_API_KEY and expose isAiConfigured() === false without throwing"
    - "Customer pricing tools only call getVehiclePrice / calculateMatPrice — never invent numbers"
    - "Rate-limit helper rejects excess calls; max_tokens and kill-switch env are readable from shared config"
    - "Customer tool module has no imports or calls into requireAdminKey / admin mutations"
  artifacts:
    - path: src/lib/ai/anthropic-client.ts
      provides: "Lazy Anthropic/AI SDK client factory mirroring convex-server.ts"
    - path: src/lib/ai/config.ts
      provides: "isAiConfigured, kill switch, per-feature max_tokens"
    - path: src/lib/ai/rate-limit.ts
      provides: "Per-IP/session rate limiter for public AI routes"
    - path: src/lib/ai/customer-tools.ts
      provides: "Read-only price tools for customer AI surface"
  key_links:
    - from: "isAiConfigured()"
      to: "ANTHROPIC_API_KEY + AI_FEATURES_ENABLED env"
      via: "src/lib/ai/config.ts"
    - from: "customer-tools"
      to: "getVehiclePrice / calculateMatPrice"
      via: "direct imports from vehicle-data.ts / mat-pricing.ts"
---

<objective>
Stand up the shared server-side AI client, cost/safety guardrails, and customer-only pricing tools that every later Phase 5–7 AI surface reuses.

Purpose: Satisfy AIINF-01…05 foundations (D-01 key placement, D-04 SDK, D-05 graceful config, D-06 cost controls, D-03/D-07 price tools + isolation) before any public vehicle-match route exists.
Output: Installed AI SDK packages, `src/lib/ai/*` helpers, vitest coverage for config/rate-limit/price tools, `.env.example` docs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ai-infrastructure-vehicle-matcher/05-CONTEXT.md
@.planning/phases/05-ai-infrastructure-vehicle-matcher/05-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@src/lib/convex-server.ts
@src/lib/vehicle-data.ts
@src/lib/mat-pricing.ts
@CLAUDE.md
</context>

<decision_map>
CONTEXT Implementation Decisions mapped for coverage:
- D-01: Customer LLM calls in Next.js `src/app/api/ai/*`; key never in browser / never NEXT_PUBLIC
- D-03: Prices only from getVehiclePrice / calculateMatPrice via tools
- D-04: Vercel AI SDK (`ai` + `@ai-sdk/anthropic`); verify npm pairing at install
- D-05: Unset/placeholder key → AI disabled; non-AI path still works
- D-06: Rate limit + max_tokens + kill switch with first public surface
- D-07: Customer tool surface isolated from requireAdminKey mutations
</decision_map>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install AI SDK and create lazy client + config</name>
  <files>package.json, package-lock.json, .env.example, src/lib/ai/anthropic-client.ts, src/lib/ai/config.ts, src/lib/ai/config.test.ts</files>
  <behavior>
    - isAiConfigured() is false when ANTHROPIC_API_KEY is missing, empty, or placeholder-like
    - isAiConfigured() is false when AI_FEATURES_ENABLED is explicitly "false" (kill switch) even if key is set
    - isAiConfigured() is true only when key looks set AND kill switch is not off
    - getAiMaxTokens("vehicle-match") returns a small capped number (discretion: 256)
    - getAnthropicProvider() / getLanguageModel() returns null when not configured (no throw)
  </behavior>
  <action>
    Install packages in one shot per D-04 and 05-RESEARCH Package Legitimacy Audit ([VERIFIED] `ai`, `@ai-sdk/anthropic`, `zod`): run `npm install ai@latest @ai-sdk/anthropic@latest zod` from repo root. Do not invent version pins — accept whatever npm resolves; confirm `ai` and `@ai-sdk/anthropic` majors are a supported pairing (if install warns about peer mismatch, install the provider version that peers with the installed `ai`). Do not add LangChain or Convex Agent packages.

    Create `src/lib/ai/config.ts` (server-safe helpers):
    - Read `ANTHROPIC_API_KEY` (never `NEXT_PUBLIC_*`) per D-01.
    - Treat empty / missing / substring `your-` or `sk-ant-placeholder` style placeholders as unset (mirror `convex-server.ts` `your-deployment` guard) per D-05 / AIINF-02.
    - Kill switch: `AI_FEATURES_ENABLED` defaults to enabled when unset; `"false"` / `"0"` disables all AI (D-06 / AIINF-04).
    - Export `isAiConfigured()`, `getAiMaxTokens(feature: "vehicle-match" | …)`, and model id constant for matcher (use cheap Haiku-class id from research, e.g. current Anthropic Haiku 4.5 slug — verify against Anthropic docs at implement time).

    Create `src/lib/ai/anthropic-client.ts` with `import "server-only"`. Lazy-init AI SDK Anthropic provider / language model factory; return null when `!isAiConfigured()`. Never log the raw API key.

    Update `.env.example` with commented `ANTHROPIC_API_KEY=` and `AI_FEATURES_ENABLED=true` plus a one-line Turkish/English note that these are server-only.

    Write vitest file covering the behaviors above (env stubbing via `vi.stubEnv` or equivalent).
  </action>
  <verify>
    <automated>npm test -- src/lib/ai/config.test.ts</automated>
  </verify>
  <done>
    Packages installed with verified pairing; `isAiConfigured` / kill switch / max_tokens work; client factory returns null when disabled (AIINF-01, AIINF-02, AIINF-04, D-01, D-04, D-05, D-06).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rate limiter and customer-only price tools</name>
  <files>src/lib/ai/rate-limit.ts, src/lib/ai/rate-limit.test.ts, src/lib/ai/customer-tools.ts, src/lib/ai/customer-tools.test.ts</files>
  <behavior>
    - checkAiRateLimit(key) allows the first N requests within a window then returns limited
    - price tool / helper for a known brand+model equals getVehiclePrice(brand, model)
    - calculateMatPrice tool / helper with heelPad/trunkMat flags equals calculateMatPrice from mat-pricing.ts
    - customer-tools module source does not import convex admin auth or any admin mutation module
  </behavior>
  <action>
    Implement `src/lib/ai/rate-limit.ts` (D-06 / AIINF-04): in-process sliding window or token bucket keyed by client IP string (and optional `x-ai-session` header later). Discretionary defaults: ~10 vehicle-match requests per IP per 60s. Export `checkAiRateLimit(key: string): { allowed: boolean; retryAfterSec?: number }`. Document in a short file header that multi-instance Vercel softness is accepted for v1.

    Implement `src/lib/ai/customer-tools.ts` (D-03, D-07 / AIINF-03, AIINF-05):
    - Pure helpers (and optional AI SDK `tool()` wrappers if useful for Plan 02) that call only `getVehiclePrice` from `@/lib/vehicle-data` and `calculateMatPrice` / `MAT_PRICING` from `@/lib/mat-pricing`.
    - Do not import anything from `convex/lib/adminAuth`, admin Convex modules, or any function that takes `adminKey`.
    - No write tools. No order/CMS mutations.

    Add vitest coverage for rate-limit exhaustion and price-equality against the real pricing functions.
  </action>
  <verify>
    <automated>npm test -- src/lib/ai/rate-limit.test.ts src/lib/ai/customer-tools.test.ts</automated>
  </verify>
  <done>
    Rate limiter blocks excess keys; customer tools are price-equality-true against code and structurally isolated from admin surfaces (AIINF-03, AIINF-04, AIINF-05, D-03, D-06, D-07).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → future `/api/ai/*` | Untrusted clients will call routes that use these helpers |
| Env secrets → Node runtime | `ANTHROPIC_API_KEY` must never reach client bundles |
| Customer tools → pricing code | Only read-only price computation allowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | anthropic-client / env | high | mitigate | `server-only` import; never use NEXT_PUBLIC for key; no key in client components |
| T-05-02 | Denial of Service | rate-limit.ts | high | mitigate | Per-IP rate limit + max_tokens caps + AI_FEATURES_ENABLED kill switch |
| T-05-03 | Elevation of Privilege | customer-tools.ts | critical | mitigate | No adminKey tools; no Convex write imports; enforce via module boundary + test grep |
| T-05-04 | Tampering | price tools | high | mitigate | Prices only from getVehiclePrice/calculateMatPrice (AIINF-03) |
| T-05-SC | Tampering | npm installs | high | mitigate | Only [VERIFIED] packages from 05-RESEARCH Package Legitimacy Audit; install ai + provider together |
</threat_model>

<verification>
- `npm test -- src/lib/ai/` passes for new tests
- `npm run typecheck` clean for new files
- `rg "NEXT_PUBLIC_.*ANTHROPIC|ANTHROPIC_API_KEY" src --glob '!**/ai/**'` should not introduce client exposure of the key
</verification>

<success_criteria>
Shared AI config/client/rate-limit/customer-tools exist, tested, and ready for Plan 02 route wiring. AIINF-01–05 foundations (except route/UI wiring and eval) are met at the library layer.
</success_criteria>

<output>
Create `.planning/phases/05-ai-infrastructure-vehicle-matcher/05-01-SUMMARY.md` when done
</output>
