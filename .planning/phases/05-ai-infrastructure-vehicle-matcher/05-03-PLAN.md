---
phase: 05-ai-infrastructure-vehicle-matcher
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - src/components/configurator/VehicleMatchInput.tsx
  - src/components/configurator/VehicleSelector.tsx
  - src/components/configurator/MatConfigurator.tsx
  - src/app/olusturucu/page.tsx
  - src/lib/ai/evals/vehicle-match-golden.ts
  - src/lib/ai/evals/vehicle-match-eval.test.ts
  - package.json
autonomous: false
requirements:
  - AIINF-02
  - AIINF-06
  - VMATCH-01
  - VMATCH-03
  - VMATCH-04
must_haves:
  truths:
    - "When AI status is unavailable, MatConfigurator shows only existing manual vehicle fields — no broken AI chrome"
    - "Ambiguous match results render a selectable candidate list in Turkish; user must choose"
    - "Choosing a candidate pre-fills brand/model (and body/year when known) into MatConfigurator vehicle state"
    - "Golden eval dataset of 20+ Turkish cases asserts price-equality and Turkish-quality heuristics; npm script runs it"
  artifacts:
    - path: src/components/configurator/VehicleMatchInput.tsx
      provides: "Free-text match UI + disambiguation"
    - path: src/lib/ai/evals/vehicle-match-golden.ts
      provides: "Golden Turkish vehicle cases"
    - path: src/lib/ai/evals/vehicle-match-eval.test.ts
      provides: "Price-equality + Turkish quality + match assertions"
  key_links:
    - from: "VehicleMatchInput onSelect"
      to: "VehicleDetails / setVehicle"
      via: "VehicleSelector or MatConfigurator"
    - from: "golden eval"
      to: "runVehicleMatch + getVehiclePrice"
      via: "vitest npm script"
---

<objective>
Ship the premium Turkish VehicleMatchInput UI (disambiguation + MatConfigurator prefill) and the AIINF-06 eval harness with a golden dataset.

Purpose: Close phase success criteria 1–5 for the customer-visible matcher and lock regressions before prompt/model changes (D-05, D-08, VMATCH-03, VMATCH-04).
Output: Wired configurator UI, golden eval + npm script, human-verifiable UX.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ai-infrastructure-vehicle-matcher/05-CONTEXT.md
@.planning/phases/05-ai-infrastructure-vehicle-matcher/05-02-PLAN.md
@src/components/configurator/MatConfigurator.tsx
@src/components/configurator/VehicleSelector.tsx
@src/components/VehicleDetailsFields.tsx
@src/lib/vehicle-compatibility.ts
@src/app/olusturucu/page.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: VehicleMatchInput UI + MatConfigurator prefill</name>
  <files>src/components/configurator/VehicleMatchInput.tsx, src/components/configurator/VehicleSelector.tsx, src/components/configurator/MatConfigurator.tsx, src/app/olusturucu/page.tsx</files>
  <action>
    Build `VehicleMatchInput` (client component) per VMATCH-01/03/04 and D-05:

    - Turkish copy: placeholder examples like "2019 Passat variant" / "19 model Egea"; labels such as "Araç metniyle bul", button "Eşleştir".
    - On submit: POST `/api/ai/vehicle-match` with the free-text query. Show loading state using existing premium patterns (no generic SaaS spinner chrome).
    - On `matched`: call `onResolved(details)` to pre-fill vehicle step (VMATCH-04). Map brand/model from result; set `bodyOrChassis` from catalog `bodyType` when present; set `year` when LLM/deterministic extract provides a 4-digit year — otherwise leave year empty so user completes required fields (isVehicleDetailsComplete still requires year).
    - On `needs_disambiguation` / multi candidates: render a list of candidate buttons (brand + displayModel + bodyType) — user must pick; never auto-select (VMATCH-03). On pick → same prefill path.
    - On `no_match` / errors / 429 / 503: Turkish inline message; keep manual dropdowns usable.
    - Mount AI entry only when configured: either prop `aiEnabled` from server (`isAiConfigured()` in `olusturucu/page.tsx`) and/or client fetch to `/api/ai/status`. If not configured, do not render the free-text AI block (AIINF-02) — VehicleDetailsFields remain the sole path.

    Design (CLAUDE.md): OLED black / glass — use `input-rich`, `btn-red-rich` or `btn-ghost-rich` + `btn-press`, `surface-glass` / `mac-glass` as appropriate; Syne/Instrument Sans; no warm gold accents; respect reduced-motion.

    Wire into `VehicleSelector` (preferred: above VehicleDetailsFields) or MatConfigurator step 01 so `onChange` updates the same `VehicleDetails` state. Preserve existing URL `marka`/`model` initial props behavior.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    Free-text match + disambiguation UI pre-fills MatConfigurator when AI enabled; hidden cleanly when not (VMATCH-01, VMATCH-03, VMATCH-04, AIINF-02, D-05).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Golden eval harness (price-equality + Turkish quality)</name>
  <files>src/lib/ai/evals/vehicle-match-golden.ts, src/lib/ai/evals/vehicle-match-eval.test.ts, package.json</files>
  <behavior>
    - At least 20 golden cases with Turkish free-text inputs and expected outcomes (matched brand/model OR needs_disambiguation OR no_match)
    - For every expected matched case, expected priceTier equals getVehiclePrice(expectedBrand, expectedModel)
    - Turkish-quality heuristic: case inputs use Turkish characters / domain terms where relevant; responses from deterministic path do not inject English stop-word spam (heuristic on any user-facing message strings in results)
    - Suite runs via npm script without requiring ANTHROPIC_API_KEY (deterministic path); optional describe.skipIf(!key) for live LLM cases
  </behavior>
  <action>
    Create golden dataset (D-08 / AIINF-06) with 20–30 cases covering: correct Turkish models, misspellings (pasat-style typos), colloquial year+model phrasing, ambiguous multi-match brands, empty/garbage input, and domain-adjacent strings that should not invent prices.

    Eval test file:
    - Runs `runVehicleMatch` (or deterministic core) against each case.
    - Asserts outcome class and brand/model when matched.
    - Asserts priceTier equals getVehiclePrice for expected pairs (AIINF-03 price-equality).
    - Lightweight Turkish check: golden inputs themselves are Turkish; any message fields returned for UI must not be predominantly English (simple heuristic).

    Add package.json script ai:eval:vehicle-match pointing at vitest run for the eval test file.

    Document in a one-line comment at top of golden file: run before prompt/model changes.
  </action>
  <verify>
    <automated>npm run ai:eval:vehicle-match</automated>
  </verify>
  <done>
    Eval harness runnable without API key; price-equality and Turkish checks cover 20+ cases (AIINF-06, D-08).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify matcher UX on /olusturucu</name>
  <files>src/components/configurator/VehicleMatchInput.tsx, src/app/olusturucu/page.tsx</files>
  <what-built>
    VehicleMatchInput wired into MatConfigurator with API-backed match/disambiguation and eval harness.
  </what-built>
  <action>
    Pause for human UX verification of the matcher on /olusturucu after Tasks 1–2. Do not change code unless the human reports issues; then fix and re-run this checkpoint.
  </action>
  <how-to-verify>
    1. With ANTHROPIC_API_KEY unset: open `/olusturucu` — free-text AI block hidden or non-breaking; manual brand/model/year/kasa fields still work.
    2. With key set and AI_FEATURES_ENABLED=true: type a clear model (e.g. known Passat string), submit — see resolved match and brand/model prefilled; complete year if needed.
    3. Type an ambiguous string that should yield multiple candidates — see list; pick one; fields update; no silent wrong guess.
    4. Confirm price shown elsewhere in configurator still matches catalog base (getVehiclePrice / calculateMatPrice), not a hallucinated number.
    5. Confirm UI looks on-brand (OLED black / glass, Turkish copy).
  </how-to-verify>
  <verify>
    <automated>npm run ai:eval:vehicle-match</automated>
  </verify>
  <done>
    Human typed approved; success criteria 1–4 confirmed on /olusturucu.
  </done>
  <resume-signal>Type "approved" or list UI issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser UI → /api/ai/vehicle-match | User-controlled free text |
| Prefill → cart/config state | Resolved catalog fields become order configuration inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10 | Tampering | VehicleMatchInput prefill | medium | mitigate | Only apply brand/model from API candidates already catalog-validated in Plan 02 |
| T-05-11 | Information Disclosure | client fetch errors | low | accept | Generic Turkish errors; no stack/key leakage — acceptable for public UX |
| T-05-12 | Repudiation | eval harness | low | accept | Eval is local/CI gate, not audit log — acceptable for v1 matcher |
</threat_model>

<verification>
- `npm run ai:eval:vehicle-match` passes
- `npm run typecheck` passes
- Human checkpoint on `/olusturucu` covers success criteria 1–4 visually
</verification>

<success_criteria>
Phase 5 customer goal met: messy Turkish text → disambiguation or match → MatConfigurator prefill; AI off → manual path; golden eval gates prompt/model changes.
</success_criteria>

<output>
Create `.planning/phases/05-ai-infrastructure-vehicle-matcher/05-03-SUMMARY.md` when done
</output>
