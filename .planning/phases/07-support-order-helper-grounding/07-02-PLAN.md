---
phase: 07-support-order-helper-grounding
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/components/support/SupportChat.tsx
  - src/app/destek/page.tsx
  - src/components/Footer.tsx
autonomous: true
requirements:
  - SUPAI-02
  - SUPAI-03
  - SUPAI-04
  - SUPAI-05
must_haves:
  truths:
    - "User can open /destek and chat in Turkish with a premium OLED/glass AI Asistan panel when AI is configured (D-07)"
    - "Off-topic handling is visible via prompt-driven redirects; UI never claims to be a human rep (SUPAI-02, D-02/D-07)"
    - "prepare_whatsapp_handoff / draft_order_summary results show a user-gesture WhatsApp CTA that opens buildWhatsAppLink — AI never auto-opens or places an order (SUPAI-03/04, D-03/D-04)"
    - "When isAiConfigured() is false, /destek shows a non-broken Turkish fallback pointing to WhatsApp/FAQ — no dead chat chrome (AIINF-02, D-06)"
    - "Messages live only in client React state for the session; no client→Convex transcript writes (SUPAI-05, D-05)"
  artifacts:
    - path: src/components/support/SupportChat.tsx
      provides: "Premium streaming support chat with WhatsApp user-send CTA"
    - path: src/app/destek/page.tsx
      provides: "Gated support page with AI Asistan disclosure + KVKK-minimization note"
    - path: src/components/Footer.tsx
      provides: "Kurumsal link to /destek"
  key_links:
    - from: "SupportChat useChat"
      to: "POST /api/ai/support"
      via: "DefaultChatTransport api + x-ai-session"
    - from: "WhatsApp CTA click"
      to: "buildWhatsAppLink(storeWhatsappNumber, draft)"
      via: "user gesture href (settings-context or page prop)"
    - from: "/destek aiEnabled"
      to: "SupportChat mount gate"
      via: "isAiConfigured() server prop"
---

<objective>
Ship the customer-facing Turkish support / order-helper surface on `/destek`: streaming chat wired to Plan 01 tools, premium OLED-glass UI, AI Asistan disclosure, uncertain-case WhatsApp handoff, and order-draft CTA where the user presses send.

Purpose: Deliver SUPAI-02…05 observable UX on top of the Plan 01 route (grounding/refusal/tools already server-side).
Output: SupportChat component, `/destek` page with AI gate + fallback, Footer entry link.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-support-order-helper-grounding/07-CONTEXT.md
@.planning/phases/07-support-order-helper-grounding/07-01-PLAN.md
@.planning/research/PITFALLS.md
@src/components/configurator/ConfiguratorChat.tsx
@src/app/api/ai/support/route.ts
@src/lib/ai/support-tools.ts
@src/lib/ai/support-prompt.ts
@src/lib/whatsapp.ts
@src/context/settings-context.tsx
@src/components/Footer.tsx
@src/components/WhatsappFloat.tsx
@src/app/olusturucu/page.tsx
@src/app/globals.css
@CLAUDE.md
</context>

<decision_map>
- D-02: Scoped refusal remains prompt-driven; UI reinforces mats/orders domain in empty state copy
- D-03: WhatsApp escalation CTA uses buildWhatsAppLink; open only on user click/tap
- D-04: Order draft review + user send — never auto-send, never call order mutations
- D-05: Client-only message list; short session; disclosure that chats are not stored long-term
- D-06: Hide/disable chat when AI unconfigured; fallback to existing WhatsApp float / iletisim paths
- D-07: surface-glass / mac-glass, Racing Red, Syne/Instrument Sans, prefers-reduced-motion, AI Asistan label

Planner discretion:
- Dedicated `/destek` route (not SiteChrome floating widget, not shared ConfiguratorChat on /olusturucu) — Phase 6 already owns configurator chat; support is a separate surface
- Reuse ConfiguratorChat patterns (useChat, DefaultChatTransport, stream error/Yeniden dene, WhatsApp draft state) but new SupportChat file — do not overload MatConfigurator provider
- Footer Kurumsal link to /destek; leave WhatsappFloat as human WhatsApp CTA (do not replace it with AI)
</decision_map>

<tasks>

<task type="auto">
  <name>Task 1: Premium SupportChat with user-send WhatsApp handoff</name>
  <files>src/components/support/SupportChat.tsx</files>
  <action>
    Per D-03/D-04/D-05/D-07: Create `src/components/support/SupportChat.tsx` (`"use client"`) modeled on `ConfiguratorChat.tsx` but pointed at `/api/ai/support` and typed with SupportChat message/tool types from Plan 01.

    Requirements:
    - Visible title/badge: "AI Asistan". Supporting Turkish copy that this is yapay zekâ for kargo/ölçü/bakım/sipariş yardımı — never human satış temsilcisi phrasing (D-07, SUPAI-02).
    - Layout: mobile-first `surface-glass` and/or `mac-glass`, Racing Red accents (`btn-red-rich` / brand-red), OLED black — native inside `.premium-site` (no white third-party chrome).
    - Stream assistant tokens; respect prefers-reduced-motion (no noisy loop animations).
    - onToolCall handlers for client-facing tools: `prepare_whatsapp_handoff` and `draft_order_summary` store draft text + show CTA; do not window.open from async tool completion alone. CTA is an `<a href={buildWhatsAppLink(...)}>` or button that navigates synchronously on click (D-03/D-04). Prefer live whatsappNumber from `useStoreSettings()` / settings-context over hardcoding siteConfig alone (aligns with live settings grounding).
    - Server tools with execute (get_support_grounding, get_mat_price) need no client mutation — let the SDK round-trip tool results.
    - Streaming error/reconnect: Turkish error + Yeniden dene; clear truncated assistant bubble on abort/disconnect (mirror ConfiguratorChat).
    - Map 429 / 503 to Turkish messages consistent with ConfiguratorChat tone.
    - Session id for x-ai-session header only (rate-limit keying) — do not POST transcripts to Convex (D-05).
    - Optional short privacy line near composer: sohbet kaydı uzun süreli saklanmaz / eğitim için kullanılmaz (SUPAI-05 disclosure, keep one sentence).

    Do not import ConfiguratorAssistantProvider, cart, or MatConfigurator. Do not call Convex order APIs.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>SupportChat streams against /api/ai/support with AI Asistan labeling, premium styling, and user-gesture WhatsApp draft send (SUPAI-03/04/05, D-03/D-04/D-07).</done>
</task>

<task type="auto">
  <name>Task 2: Mount /destek behind AI gate + Footer entry</name>
  <files>src/app/destek/page.tsx, src/components/Footer.tsx</files>
  <action>
    Per D-06/D-07: Create `src/app/destek/page.tsx` as a server component inside the premium storefront chrome. Call `isAiConfigured()` (same pattern as `/olusturucu`). When true, render SupportChat with Turkish page H1/supporting line focused on kargo, ölçü, bakım, sipariş yardımı (hero budget: brand-safe, one headline, one short sentence, chat as the interaction surface — no dashboard chrome). When false, render a graceful Turkish fallback: explain AI Asistan unavailable, link to `/#sss`, `/bilgiler/kargo`, `/iletisim`, and/or WhatsApp via settings — never a broken empty chat shell (AIINF-02).

    Add metadata title/description in Turkish suitable for support helper.

    Update `Footer.tsx` KURUMSAL_LINKS (or adjacent) to include `{ href: "/destek", label: "AI Destek" }` (or "Sipariş Asistanı") so the surface is reachable. Do not replace WhatsappFloat.

    Ensure page is wrapped by existing SiteChrome / premium-site layout (no admin styles). Prefer static generation or default App Router behavior — no new Convex transcript tables.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>/destek mounts gated SupportChat or fallback; Footer links to it (SUPAI-02/05 UX entry, D-06/D-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SupportChat → /api/ai/support | Client messages are untrusted; tools may return drafts |
| WhatsApp CTA → wa.me | User-initiated only; draft text may include PII user typed |
| /destek public page | No admin session required; must not expose admin keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Spoofing | SupportChat copy | medium | mitigate | Permanent AI Asistan label + anti-human-rep copy |
| T-07-08 | Elevation / Tampering | WhatsApp + order draft | high | mitigate | User-gesture only; no orders.create; no auto window.open from tool callback |
| T-07-09 | Information disclosure | client transcript | medium | mitigate | Session-scoped React state only; privacy one-liner; no Convex writes |
| T-07-10 | Denial of service | chat UX | low | accept | Reuse existing rate-limit errors; Yeniden dene is bounded client retry |
</threat_model>

<verification>
- `npm run typecheck` passes
- Focused lint on `src/components/support/**` and `src/app/destek/**`
- Smoke: `/destek` present in `npm run build` route list when executed later in Plan 03
</verification>

<success_criteria>
- /destek offers gated premium support chat with user-send WhatsApp drafts
- AI-off path remains usable via FAQ/WhatsApp links
- No server transcript persistence from the UI layer
</success_criteria>

<output>
Create `.planning/phases/07-support-order-helper-grounding/07-02-SUMMARY.md` when done
</output>
