---
phase: 08-admin-content-generator
plan: 03
type: execute
wave: 3
depends_on:
  - "08-02"
files_modified:
  - src/lib/ai/evals/content-generator-golden.ts
  - src/lib/ai/evals/content-generator-eval.test.ts
  - package.json
  - src/app/admin/icerik/ContentGeneratorPanel.tsx
  - src/app/admin/icerik/ContentManager.tsx
autonomous: false
requirements:
  - CNTGEN-01
  - CNTGEN-02
  - CNTGEN-03
  - CNTGEN-04
  - AIINF-02
must_haves:
  truths:
    - "Golden eval asserts CONTENT_STYLE_GUIDE / CONTENT_SYSTEM_PROMPT encode Turkish premium voice constraints (D-04, CNTGEN-04)"
    - "Golden eval asserts grounding pack includes catalog price/features for a known slug and does not invent unknown-slug specs (D-01, CNTGEN-01)"
    - "Golden eval asserts generation route/helper sources do not call live CMS/product write helpers — publish is a separate action (D-02, CNTGEN-02)"
    - "Eval asserts ContentManager embed path + admin-key usage patterns for the generator panel (D-03, CNTGEN-03)"
    - "npm script ai:eval:content-generator gates regressions without ANTHROPIC_API_KEY"
    - "Human confirms draft-then-publish, grounding, brand voice, AI-off fallback on /admin/icerik after login"
  artifacts:
    - path: src/lib/ai/evals/content-generator-golden.ts
      provides: "Golden fixtures for style, grounding, no-live-write"
    - path: src/lib/ai/evals/content-generator-eval.test.ts
      provides: "Vitest runner for content generator evals"
  key_links:
    - from: "ai:eval:content-generator"
      to: "content-generator-eval.test.ts"
      via: "package.json"
    - from: "generate route source scan"
      to: "absence of live CMS write helpers"
      via: "eval assertions"
---

<objective>
Lock CNTGEN-01…04 (+ AIINF-02 carryover) with deterministic golden evals and a blocking human verification checklist on `/admin/icerik`.

Purpose: Prevent auto-publish regressions, invented specs, and brand-voice drift before milestone close; mirror Phase 5–7 eval gates.
Output: content-generator golden eval + npm script, optional UX polish, human-verify checkpoint (admin login required).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-admin-content-generator/08-CONTEXT.md
@.planning/phases/07-support-order-helper-grounding/07-03-PLAN.md
@src/lib/ai/evals/support-chat-golden.ts
@src/lib/ai/evals/support-chat-eval.test.ts
@src/lib/ai/content-style-guide.ts
@src/lib/ai/content-prompt.ts
@src/lib/ai/content-grounding.ts
@src/lib/ai/content-generate.ts
@src/app/api/admin/ai/content/route.ts
@src/app/admin/icerik/actions.ts
@src/app/admin/icerik/ContentGeneratorPanel.tsx
@src/app/admin/icerik/ContentManager.tsx
@package.json
</context>

<decision_map>
- D-01: Grounding eval mandatory (known slug facts; unknown slug no invention)
- D-02: Critical safety eval — generation writes no live content (source/contract scan)
- D-03: Panel embedded under /admin/icerik; admin-key patterns present
- D-04: Style-guide constraints present in prompt constants
- D-05: AI-off behavior covered in human checklist
Discretion: Keep evals deterministic without live Anthropic calls (mirror support-chat eval). Live generation quality remains human-verified after admin login.
</decision_map>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Content generator golden eval + npm script</name>
  <files>src/lib/ai/evals/content-generator-golden.ts, src/lib/ai/evals/content-generator-eval.test.ts, package.json</files>
  <behavior>
    - Style-guide: CONTENT_STYLE_GUIDE and content system prompt contain Turkish-output and premium-register constraints; banned-phrasing / no-spammy-sales rules are detectable as documented substrings or structured markers from Plan 01
    - Grounding: buildContentGroundingFacts for a known product slug includes that product's catalog price and at least one real feature/name field; unknown slug does not invent a fake price/name
    - No-live-write (CNTGEN-02): source-scan content-generate.ts and api/admin/ai/content/route.ts for absence of live write helper identifiers used by CMS publish (e.g. saveFaqItem, saveSiteSeo, saveContentSection, saveContentPage) and products.update call sites; publish action file is allowed to contain those
    - Isolation: generate route requires auth gate symbol (isAuthenticated) and uses contentGenerations upsert for persistence
    - Suite passes with ANTHROPIC_API_KEY unset
  </behavior>
  <action>
    Per D-01, D-02, D-04: Mirror Phase 7 eval layout. Add `content-generator-golden.ts` documenting style markers, grounding fixtures (known + unknown slug), and no-live-write contract notes.

    Add `content-generator-eval.test.ts` that asserts prompt constants, runs grounding fixtures, and source-scans generate helper + route. Add package.json script `ai:eval:content-generator` alongside existing `ai:eval:*` scripts.

    Do not require live LLM calls. Do not expand into bulk catalog generation (CONTEXT deferred).
  </action>
  <verify>
    <automated>npm run ai:eval:content-generator</automated>
  </verify>
  <done>Golden eval passes without API key; style, grounding, and no-live-write scans green (CNTGEN-01/02/04).</done>
</task>

<task type="auto">
  <name>Task 2: Admin generator polish from eval/smoke gaps</name>
  <files>src/app/admin/icerik/ContentGeneratorPanel.tsx, src/app/admin/icerik/ContentManager.tsx</files>
  <action>
    After Task 1 and a local smoke of `/admin/icerik` (dev server if available, with admin login): fix remaining UX gaps — clear Turkish labels for draft vs publish, error states for 401/429/503, empty Convex/draft messaging, disabled generate when aiAvailable false, ensure publish button disabled until a ready draft is selected/edited. Do not add customer polish classes. Do not implement bulk generation or image generation (deferred).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Admin generator UX polish complete and typechecks (CNTGEN-03).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify Phase 8 success criteria</name>
  <files>src/app/admin/icerik/ContentManager.tsx, src/app/admin/icerik/ContentGeneratorPanel.tsx</files>
  <what-built>
    Admin content generator inside /admin/icerik: grounded Turkish product/SEO/FAQ drafts, explicit publish to live CMS, admin-key-gated, premium style-guide tone, AI-off safe.
  </what-built>
  <action>
    Pause for human verification of Phase 8 ROADMAP success criteria and CNTGEN-01…04 on /admin/icerik. Do not mark the phase complete until the resume signal is received. If issues are reported, fix them in follow-up commits before closing the plan.
  </action>
  <how-to-verify>
    Prerequisites: valid admin login (admin_session cookie via /admin/login). Note: /admin/* redirects unauthenticated users — complete login first.

    1. With ANTHROPIC_API_KEY set and AI_FEATURES_ENABLED true: open /admin/icerik — confirm AI Taslak (or equivalent) tab is visible inside ContentManager (CNTGEN-03).
    2. Generate a product_description for a known product slug — draft appears for review; live product description on storefront/admin products is unchanged until publish (CNTGEN-02).
    3. Confirm draft text is Turkish and premium in tone (no spammy marketplace voice) (CNTGEN-04).
    4. Confirm generated copy reflects real product/vehicle facts (name, price tier/features) and does not invent unrelated specs (CNTGEN-01).
    5. Explicitly click publish — only then does live CMS/product content update; regenerate alone must not publish (CNTGEN-02).
    6. Repeat for SEO and FAQ kinds at least once each (or confirm UI exposes all three kinds).
    7. With AI disabled / key unset: generator tab hidden or disabled; existing SEO/FAQ/section editors still work (AIINF-02).
    8. Visual: plain admin UI (sharp corners, no customer glass/rich classes); mobile admin layout usable.
  </how-to-verify>
  <verify>
    <automated>npm run ai:eval:content-generator</automated>
    <human-check>All how-to-verify steps pass on /admin/icerik after login</human-check>
  </verify>
  <done>Human approved Phase 8 success criteria (CNTGEN-01…04 observable on /admin/icerik).</done>
  <resume-signal>Type "approved" or list issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Eval harness → generate/publish sources | Deterministic scans; no live model trust for safety property |
| Human verify → production readiness | Catches tone/UX/auth gaps automation cannot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09 | Tampering | Auto-publish regression | critical | mitigate | Eval source-scan fails if generate path gains live CMS writes |
| T-08-10 | Repudiation | Human checklist skip | medium | accept | Blocking checkpoint required before phase complete; documented in SUMMARY |
| T-08-SC | Tampering | npm installs | high | mitigate | No new packages in this plan |
</threat_model>

<verification>
- `npm run ai:eval:content-generator` passes without ANTHROPIC_API_KEY
- `npm run typecheck` passes
- Human checklist completed after admin login
</verification>

<success_criteria>
CNTGEN-01…04 locked by eval gate + human approval; milestone v1.1 Phase 8 ready to close after approve.
</success_criteria>

<output>
Create `.planning/phases/08-admin-content-generator/08-03-SUMMARY.md` when done
</output>
